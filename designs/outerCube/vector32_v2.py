#!/usr/bin/env python3
"""
vector32_v2.py — VEC-4K-v2 datapath analyser + ROW_SUM_EXP microcode generator.

Models (per `vector4k_v2.md`):
  - 4 KB TILE = R × C elements with element format E (bytes/elem); R·C·E = 4096
    Supported formats : FP8, FP16, FP32, FP64  (FP4 in spec, omitted here)
    Example tiles     : (64, 64, FP8) (32, 64, FP16) (64, 2, FP32) (1, 256, FP16)
  - 4 register files (each 4 KB, organised as 2 K × 2 B DFFs):
        RFS_A — operand-A staging
        RFS_B — operand-B staging  (REPURPOSED here as in-place accumulator
                                    + later as output-tile buffer for ROW_SUM_EXP)
        RFA_A — accumulator RF half 0   (legacy Acc ping-pong, §5.5)
        RFA_B — accumulator RF half 1   (legacy Acc ping-pong, §5.5)
  - 512 B/cy tilelet read port — per cycle delivers one 512 B tilelet from a
    register file. Beats are SEQUENTIAL row-major:
        beat 0 : bytes    0 ..  511
        beat 1 : bytes  512 .. 1023
        ...
        beat 7 : bytes 3584 .. 4095

Tree datapath (Benes-like shrink-then-expand network)
  - INVERTED REDUCTION TREE  : 1024 B input width = 2 × 512 B (operand A + B)
        log2(N_in) levels of binary nodes, each {ADD, CMP_SWAP_MAX,
        CMP_SWAP_MIN, PASS_LEFT, PASS_RIGHT, MUX_BY_CTRL}.
        A per-level "stop bit" lets a sub-tree deliver multiple partial outputs
        (one per sub-tree, e.g. one per row in a row reduce).
  - FORWARD BROADCAST TREE   : 1024 B output width = 2 × 512 B
        log2(N_out) levels of binary FANOUT nodes (each 1 → 2 splitter,
        optionally ADD_THEN_FANOUT / CMP_SWAP_THEN_FANOUT).
  - Combined : REDUCE → (optional permute) → BROADCAST.
        Same hardware also implements axis reduce (sum/max/min), broadcast
        (col / row expand), sort, and merge_sort (CMP_SWAP at internal nodes
        + SHUFFLE at the BROADCAST output).

Accumulator policy
  Q : do we need a dedicated 4 KB accumulator?
  A : NO.  R · E_out bytes is enough (≤ 1 KB for canonical shapes), and we
      reuse RFS_B (operand-B staging register) as the in-place accumulator.
      A small ACC_SPILL latch (R · E_out bytes wide) is read out before the
      broadcast phase begins, so we get clean read-before-write semantics
      even though phase-2 broadcast overwrites RFS_B end-to-end.

Worked example : ROW_SUM_EXP
  Sum each row of an input TILE, then broadcast the sum to every column.
  D[r, c] = Σ_{c'=0..C-1} A[r, c']    for all r ∈ [0, R), c ∈ [0, C_out)
  Output element format E_out is selectable :
      E_out = E_in            → out shape (R, C    , E_in)   — same precision
      E_out = 2·E_in          → out shape (R, C/2  , 2·E_in) — promoted to next
                                  level; output tile still 4 KB.

CLI
  python3 vector32_v2.py                       # INTERACTIVE: prompt R / in_fmt / out_fmt
  python3 vector32_v2.py --tile 8 128 FP32     # non-interactive, single shape
  python3 vector32_v2.py --tile 8 256 FP16 --double-width
  python3 vector32_v2.py --with-context        # interactive + overview + tree + pseudo
  python3 vector32_v2.py --all-examples        # dump 5 canonical examples (legacy mode)
  python3 vector32_v2.py --overview-only       # just overview
  python3 vector32_v2.py --pseudo-only         # overview + tree + pseudo, no trace
  python3 vector32_v2.py --operand-layout      # parallel vs interleaved analysis
  python3 vector32_v2.py --sort-network        # bitonic sort net (shared design)
  python3 vector32_v2.py --accumulator-design  # acc FF design (sharing, FB, sizing)
  python3 vector32_v2.py --topk-argmax         # TOP-K and ARG-MAX: SORT or REDUCE?
  python3 vector32_v2.py --unary-reduce        # FINAL design: unary REDUCE +
                                               #   RFS_B-as-acc + 512 B half tree
  python3 vector32_v2.py --time-muxed-sort     # FINAL sort: 1-stage time-muxed
                                               #   (16x area reduction, same lat)
  python3 vector32_v2.py --final-diagram       # FINAL full-system block diagram
                                               #   with all revisions applied
"""

from __future__ import annotations

import argparse
import math
from dataclasses import dataclass
from typing import List, Tuple

# ---------------------------------------------------------------------------
# Constants — datapath
# ---------------------------------------------------------------------------

TILE_BYTES        = 4096
TILELET_BYTES     = 512
NUM_BEATS         = TILE_BYTES // TILELET_BYTES   # 8
RF_BYTES          = TILE_BYTES                    # one RF stores one tile
DFF_WIDTH_B       = 2
DFF_PER_RF        = RF_BYTES // DFF_WIDTH_B       # 2048

TREE_INPUT_BYTES  = 2 * TILELET_BYTES             # 1024 B (2 operand inputs)
TREE_OUTPUT_BYTES = 2 * TILELET_BYTES             # 1024 B (broadcast width)


# ---------------------------------------------------------------------------
# Constants — formats
# ---------------------------------------------------------------------------

FORMAT_E = {           # bytes per element (FP4 in spec; omitted from this script)
    'FP8':  1,
    'FP16': 2,
    'FP32': 4,
    'FP64': 8,
}

NEXT_FORMAT = {        # double-width promotion
    'FP8':  'FP16',
    'FP16': 'FP32',
    'FP32': 'FP64',
    'FP64': None,
}

REGISTER_FILES = [
    ("RFS_A", "Operand-A staging RF      — input tile A"),
    ("RFS_B", "Operand-B staging RF      — REPURPOSED:  acc[] + output tile D"),
    ("RFA_A", "Accumulator RF A          — Acc ping-pong half 0 (legacy §5.5)"),
    ("RFA_B", "Accumulator RF B          — Acc ping-pong half 1 (legacy §5.5)"),
]


# ---------------------------------------------------------------------------
# TileSpec
# ---------------------------------------------------------------------------

@dataclass
class TileSpec:
    R: int
    C: int
    fmt: str

    @property
    def E(self) -> int:
        return FORMAT_E[self.fmt]

    @property
    def total_bytes(self) -> int:
        return self.R * self.C * self.E

    @property
    def row_bytes(self) -> int:
        return self.C * self.E

    def validate(self) -> None:
        if self.fmt not in FORMAT_E:
            raise ValueError(f"Unsupported format {self.fmt!r}; "
                             f"choose from {list(FORMAT_E)}")
        if self.R <= 0 or self.C <= 0:
            raise ValueError(f"R and C must be positive, got {self.R}×{self.C}")
        if self.total_bytes != TILE_BYTES:
            raise ValueError(
                f"Tile spec ({self.R}×{self.C}, {self.fmt}) = "
                f"{self.total_bytes} B, expected {TILE_BYTES} B"
            )

    def __str__(self) -> str:
        return f"{self.R}×{self.C} {self.fmt}"


# ---------------------------------------------------------------------------
# RowSumExpPlan
# ---------------------------------------------------------------------------

@dataclass
class RowSumExpPlan:
    in_spec: TileSpec
    out_spec: TileSpec

    # ---- regime helpers ----
    @property
    def E_in(self) -> int:
        return self.in_spec.E

    @property
    def E_out(self) -> int:
        return self.out_spec.E

    @property
    def row_bytes_in(self) -> int:
        return self.in_spec.row_bytes

    @property
    def row_bytes_out(self) -> int:
        return self.out_spec.row_bytes

    @property
    def regime_in(self) -> str:
        # A : multiple full rows per input beat (or exactly 1)
        # B : input row spans multiple beats
        return "A" if self.row_bytes_in <= TILELET_BYTES else "B"

    @property
    def regime_out(self) -> str:
        return "A" if self.row_bytes_out <= TILELET_BYTES else "B"

    @property
    def rows_per_input_beat(self) -> int:
        return TILELET_BYTES // self.row_bytes_in if self.regime_in == "A" else 0

    @property
    def beats_per_row_in(self) -> int:
        return self.row_bytes_in // TILELET_BYTES if self.regime_in == "B" else 1

    @property
    def rows_per_output_beat(self) -> int:
        return TILELET_BYTES // self.row_bytes_out if self.regime_out == "A" else 0

    @property
    def beats_per_row_out(self) -> int:
        return self.row_bytes_out // TILELET_BYTES if self.regime_out == "B" else 1

    @property
    def num_reduce_beats(self) -> int:
        return NUM_BEATS

    @property
    def num_broadcast_beats(self) -> int:
        return NUM_BEATS

    @property
    def acc_bytes(self) -> int:
        return self.in_spec.R * self.E_out

    # ---- tree depth ----
    @property
    def reduce_subtrees_per_beat(self) -> int:
        return self.rows_per_input_beat if self.regime_in == "A" else 1

    @property
    def reduce_elems_per_subtree(self) -> int:
        return self.in_spec.C if self.regime_in == "A" else (TILELET_BYTES // self.E_in)

    @property
    def reduce_tree_levels(self) -> int:
        n = self.reduce_elems_per_subtree
        return int(math.ceil(math.log2(n))) if n > 1 else 0

    @property
    def broadcast_subtrees_per_beat(self) -> int:
        return self.rows_per_output_beat if self.regime_out == "A" else 1

    @property
    def broadcast_elems_per_subtree(self) -> int:
        return self.out_spec.C if self.regime_out == "A" else (TILELET_BYTES // self.E_out)

    @property
    def broadcast_tree_levels(self) -> int:
        n = self.broadcast_elems_per_subtree
        return int(math.ceil(math.log2(n))) if n > 1 else 0


def make_plan(in_spec: TileSpec, double_width: bool = False) -> RowSumExpPlan:
    in_spec.validate()
    if double_width:
        out_fmt = NEXT_FORMAT.get(in_spec.fmt)
        if out_fmt is None:
            raise ValueError(f"No double-width promotion above {in_spec.fmt}")
        if in_spec.C % 2 != 0:
            raise ValueError(
                f"Cannot double-width: C = {in_spec.C} not divisible by 2 "
                f"(would need C_out = C / 2)")
        out_spec = TileSpec(R=in_spec.R, C=in_spec.C // 2, fmt=out_fmt)
    else:
        out_spec = TileSpec(R=in_spec.R, C=in_spec.C, fmt=in_spec.fmt)
    out_spec.validate()
    return RowSumExpPlan(in_spec=in_spec, out_spec=out_spec)


# ---------------------------------------------------------------------------
# Pretty-print helpers
# ---------------------------------------------------------------------------

def hbar(width: int = 78, char: str = "═") -> str:
    return char * width


def sub(width: int = 78, char: str = "─") -> str:
    return char * width


# ---------------------------------------------------------------------------
# Reports
# ---------------------------------------------------------------------------

def print_overview() -> None:
    print(hbar())
    print("  VEC-4K-v2 datapath overview  (analyser model — see vector4k_v2.md)")
    print(hbar())
    print(f"  TILE size              : {TILE_BYTES} B  "
          f"= R · C · E   (E ∈ {{1, 2, 4, 8}} B for FP8 / FP16 / FP32 / FP64)")
    print(f"  Per register file      : {RF_BYTES} B = {DFF_PER_RF} × "
          f"{DFF_WIDTH_B} B DFFs")
    print(f"  Tilelet (read port)    : {TILELET_BYTES} B / cycle, beats are "
          f"sequential row-major")
    print(f"                           beat 0 : bytes    0..{TILELET_BYTES-1}")
    print(f"                           beat 1 : bytes  {TILELET_BYTES}.."
          f"{2*TILELET_BYTES-1}")
    print(f"                           ...")
    print(f"                           beat 7 : bytes 3584..4095")
    print(f"  Beats per tile         : {NUM_BEATS}")
    print()
    print(f"  Register files (4 × {RF_BYTES} B = 16 KB total) :")
    for name, desc in REGISTER_FILES:
        print(f"    • {name}  : {desc}")
    print()
    print("  Sample tile shapes (each = 4 KB) :")
    for spec in [(64, 64, 'FP8'), (32, 64, 'FP16'), (64, 2, 'FP32'),
                 (1, 256, 'FP16'), (8, 128, 'FP32'), (1, 1024, 'FP32'),
                 (256, 4, 'FP32'), (16, 128, 'FP16')]:
        ts = TileSpec(*spec)
        try:
            ts.validate()
            print(f"    • ({ts.R:>4} × {ts.C:<4}, {ts.fmt:<4})  → "
                  f"row_bytes = {ts.row_bytes} B")
        except ValueError:
            pass
    print()


def print_tree_topology() -> None:
    print(hbar())
    print("  TREE DATAPATH  (Benes-like shrink-then-expand network)")
    print(hbar())
    print()
    print("  INPUT side  (1024 B = 2 × 512 B from RFS_A and RFS_B / Acc):")
    print()
    print("      RFS_A 512 B tilelet              RFS_B 512 B tilelet  (= acc[] tilelet)")
    print("              │                                    │")
    print("              ▼                                    ▼")
    print("       ┌────────────────────────────────────────────────────┐")
    print("       │  L0 leaves :  N_in  bytes / elements               │")
    print(f"       │  N_in = {TREE_INPUT_BYTES} / E   (E = bytes/elem)              │")
    print("       │  → FP8 : 1024 leaves    FP16 : 512 leaves          │")
    print("       │  → FP32:  256 leaves    FP64 : 128 leaves          │")
    print("       └────────────────────────────────────────────────────┘")
    print("              │")
    print("              ▼")
    print("       ┌────────────────────────────────────────────────────┐")
    print("       │  L1 .. L_log2(N_in) : reduction stages             │")
    print("       │    each stage = N/2 binary nodes                   │")
    print("       │    node ops : ADD | CMP_SWAP_MAX | CMP_SWAP_MIN |  │")
    print("       │               PASS_LEFT | PASS_RIGHT | MUX_BY_CTRL │")
    print("       │    per-level 'stop bit' : let sub-trees deliver    │")
    print("       │      K partial outputs at any chosen level.        │")
    print("       │    ⇒ for ROW_SUM_EXP  : K = rows_per_input_beat,   │")
    print("       │      sub-tree size = C input elements per row.     │")
    print("       └────────────────────────────────────────────────────┘")
    print("              │")
    print("              ▼")
    print("        K partial outputs at chosen stop level")
    print("        (K = rows in this beat ; each = 1 element of E_out precision)")
    print()
    print()
    print("  OUTPUT side (1024 B = 2 × 512 B back into RFS_B):")
    print()
    print("         ACC_SPILL[r] (1 element per row, E_out wide)")
    print("              │")
    print("              ▼")
    print("       ┌────────────────────────────────────────────────────┐")
    print("       │  L0 seeds :  K elements (one per output row)       │")
    print("       └────────────────────────────────────────────────────┘")
    print("              │")
    print("              ▼")
    print("       ┌────────────────────────────────────────────────────┐")
    print("       │  L1 .. L_log2(N_out) : broadcast stages            │")
    print("       │    each stage = 2× wider than previous             │")
    print("       │    node ops : FANOUT | ADD_THEN_FANOUT |           │")
    print("       │               CMP_SWAP_THEN_FANOUT | PERMUTE       │")
    print("       └────────────────────────────────────────────────────┘")
    print("              │")
    print("              ▼")
    print("       ┌────────────────────────────────────────────────────┐")
    print("       │  Lout leaves : N_out bytes / elements              │")
    print("       │  ⇒ ROW_SUM_EXP : C_out copies per row × K rows     │")
    print("       │    = 1024 B (or 512 B for one tilelet output)      │")
    print("       └────────────────────────────────────────────────────┘")
    print()
    print("  Combined REDUCE → BROADCAST  ≡  Benes-like network with arithmetic at the")
    print("  internal nodes. The same hardware also implements:")
    print()
    print("    OPERATION          | REDUCE side ops                  | BROADCAST side ops")
    print("    -------------------|----------------------------------|---------------------")
    print("    axis reduce sum    | ADD                              | (none — no broadcast)")
    print("    axis reduce max    | CMP_SWAP_MAX (keep larger)       | (none)")
    print("    axis reduce min    | CMP_SWAP_MIN (keep smaller)      | (none)")
    print("    column / row expand| (none — no reduce)               | FANOUT")
    print("    ROW_SUM_EXP        | ADD                              | FANOUT")
    print("    sort               | CMP_SWAP (full bitonic schedule) | PERMUTE")
    print("    merge_sort         | CMP_SWAP between two halves      | PERMUTE / FANOUT")
    print()


# ---------------------------------------------------------------------------
# Benes-like detailed tree diagram
# ---------------------------------------------------------------------------

def print_benes_diagram() -> None:
    print(hbar())
    print("  BENES-LIKE TREE DATAPATH — detailed structure")
    print("  (drawn for N = 16 leaves; real datapath has N = 1024 / E_in leaves)")
    print(hbar())
    print()
    print("  Per-format leaf count and stage depth:")
    print()
    print("      format   N_leaves  reduce stages  broadcast stages   total cy.")
    print("      ──────   ────────  ─────────────  ────────────────   ─────────")
    print("      FP8         1024     10               10                21*")
    print("      FP16         512      9                9                19*")
    print("      FP32         256      8                8                17*")
    print("      FP64         128      7                7                15*")
    print()
    print("      * = log2(N) reduce + 1 ACC_SPILL + log2(N) broadcast,")
    print("          but for ROW_SUM_EXP we always issue 8 reduce + 1 spill")
    print("          + 8 broadcast = 17 cycles regardless of N (compute is")
    print("          beat-pipelined; the tree depth is its critical path,")
    print("          not its issue rate).")
    print()
    print("  ─── REDUCE half  (binary fan-in, log2 N ADD/CMP_SWAP stages) ──────────")
    print()
    print("    Operand-A tilelet (512 B from RFS_A)")
    print("              +  Operand-B / Acc tilelet (512 B from RFS_B)")
    print("              =  N leaves total at L0")
    print()
    print("  L0    ● ● ● ● ● ● ● ● ● ● ● ● ● ● ● ●    leaves  (E_in-wide elements)")
    print("        └┬┘ └┬┘ └┬┘ └┬┘ └┬┘ └┬┘ └┬┘ └┬┘")
    print("  L1     ◉   ◉   ◉   ◉   ◉   ◉   ◉   ◉    N/2 = 8  ALU nodes")
    print("         └─┬─┘   └─┬─┘   └─┬─┘   └─┬─┘")
    print("  L2       ◉       ◉       ◉       ◉      N/4 = 4  ALU nodes")
    print("           └───┬───┘       └───┬───┘")
    print("  L3           ◉               ◉          N/8 = 2  ALU nodes")
    print("               └───────┬───────┘")
    print("  L4                   ◉                  N/16 = 1 ROOT node")
    print("                       │")
    print("                       ▼")
    print("                  1 scalar (cast E_in → E_out)")
    print()
    print("    Each ALU node :  2-input → 1-output  (\"butterfly cell\")")
    print("    opcode ∈ { ADD, CMP_SWAP_MAX, CMP_SWAP_MIN,")
    print("               PASS_LEFT, PASS_RIGHT, MUX_BY_CTRL }")
    print()
    print("    \"Stop-level\" mechanism (key for ROW_SUM_EXP with K rows / beat):")
    print("      - K=1 → take output at L4 (ROOT)   : 1 partial of N elems  reduced")
    print("      - K=2 → take outputs at L3         : 2 partials of N/2 each")
    print("      - K=4 → take outputs at L2         : 4 partials of N/4 each ★")
    print("              (e.g. (32,32,FP32) → 4 rows × 32 elems / beat)")
    print("      - K=8 → take outputs at L1         : 8 partials of N/8 each ★")
    print("              (e.g. (64,64,FP8)  → 8 rows × 64 elems / beat)")
    print()
    print("  ─── ACC_SPILL latch  (K-element wide register between halves) ─────────")
    print()
    print("                       ┌──────────────────────────────┐")
    print("                       │  K · E_out byte register     │")
    print("                       │  K = rows_per_input_beat     │")
    print("                       │  (≤ 1 KB for canonical cases)│")
    print("                       └──────────────────────────────┘")
    print("                       │  hides phase-1 → phase-2     │")
    print("                       │  RAW hazard on RFS_B         │")
    print("                       ▼")
    print()
    print("  ─── BROADCAST half  (binary fan-out, log2 N FANOUT stages) ────────────")
    print()
    print("  L4'                  ◎                  1 SEED  (acc[r] from spill)")
    print("               ┌───────┴───────┐")
    print("  L3'          ◎               ◎          2 FANOUT")
    print("           ┌───┴───┐       ┌───┴───┐")
    print("  L2'      ◎       ◎       ◎       ◎      4 FANOUT")
    print("         ┌─┴─┐   ┌─┴─┐   ┌─┴─┐   ┌─┴─┐")
    print("  L1'    ◎   ◎   ◎   ◎   ◎   ◎   ◎   ◎    8 FANOUT")
    print("        ┌┴┐ ┌┴┐ ┌┴┐ ┌┴┐ ┌┴┐ ┌┴┐ ┌┴┐ ┌┴┐")
    print("  L0'   ● ● ● ● ● ● ● ● ● ● ● ● ● ● ● ●   N=16 output leaves (E_out)")
    print("        ▼ ▼ ▼ ▼ ▼ ▼ ▼ ▼ ▼ ▼ ▼ ▼ ▼ ▼ ▼ ▼")
    print()
    print("    Output tilelet (1024 B back to RFS_B)")
    print()
    print("    Each FANOUT node :  1-input → 2-output  (\"inverse butterfly cell\")")
    print("    opcode ∈ { FANOUT,            (default — y0 = y1 = x)")
    print("               ADD_THEN_FANOUT,   (y0 = x+c0, y1 = x+c1)")
    print("               CMP_SWAP_THEN_FANOUT,")
    print("               PERMUTE          } (used by sort / merge_sort)")
    print()
    print("    Same \"stop-level\" lets BROADCAST start from any level :")
    print("      - K=1 seed at L4'  → fan out to all N leaves")
    print("      - K=4 seeds at L2' → each fans out to N/4 = 4 leaves ★")
    print("              (e.g. (32,32,FP32) → 4 rows × 32 copies / beat)")
    print()
    print("  ─── Node-internal architecture ────────────────────────────────────────")
    print()
    print("    REDUCE node  (2-in → 1-out)        BROADCAST node  (1-in → 2-out)")
    print()
    print("       a ──┐                                    ┌── y0")
    print("           ├─►─[ ALU ]─►─ y           x ──►─────┤")
    print("       b ──┘     ▲                              └── y1")
    print("                 │                              ▲")
    print("              opcode                            │")
    print("                                             opcode")
    print("    opcode ∈ {ADD, CMP_SWAP_*,         opcode ∈ {FANOUT,")
    print("              PASS_*, MUX_BY_CTRL}              ADD_THEN_FANOUT,")
    print("                                                CMP_SWAP_THEN_FANOUT,")
    print("                                                PERMUTE}")
    print()
    print("    Per-stage gate count ≈ N/2 cells × (adder + ctrl mux + opcode latch)")
    print("    Total tree gates    ≈ 2 · (N − 1) cells = O(N)")
    print()
    print("  ─── Recursive Benes(N) structure (for context / reference) ────────────")
    print()
    print("    A pure Benes(N) network is 2·log2(N) − 1 stages of 2×2 switches,")
    print("    arranged recursively :")
    print()
    print("      ┌──────────────────────────────────────────────────────────────┐")
    print("      │                                                              │")
    print("      │  in[0..N/2−1] ─┬─[2×2]─┬─►─ Benes(N/2) ─┬─[2×2]─┬─ out[..]   │")
    print("      │                │       │                │       │            │")
    print("      │                │       ▼                ▼       │            │")
    print("      │            cross    middle           middle   cross          │")
    print("      │           permute  sub-block       sub-block permute         │")
    print("      │                │       ▲                ▲       │            │")
    print("      │                │       │                │       │            │")
    print("      │  in[N/2..N−1] ─┴─[2×2]─┴─►─ Benes(N/2) ─┴─[2×2]─┴─ out[..]   │")
    print("      │                                                              │")
    print("      └──────────────────────────────────────────────────────────────┘")
    print()
    print("    Our REDUCE → SPILL → BROADCAST tree is the same back-to-back")
    print("    butterfly shape, but :")
    print("      • each 2×2 switch is replaced by an arithmetic node")
    print("        (ADD / CMP_SWAP / FANOUT instead of pure routing),")
    print("      • the central Benes(N/2) sub-block is collapsed into a single")
    print("        ACC_SPILL register (K · E_out bytes wide).")
    print()
    print("    The same hardware therefore covers reduce, broadcast, sort, and")
    print("    merge_sort by selecting per-stage opcodes from microcode, exactly")
    print("    as a permutation Benes covers any permutation by selecting per-")
    print("    switch settings from a routing table.")
    print()
    print("  ─── Wiring back to RFS_A / RFS_B (vector4k_v2.md §4.2) ────────────────")
    print()
    print("           RFS_A (4 KB)        RFS_B (4 KB)")
    print("              │                    │")
    print("              │ 512 B/cy           │ 512 B/cy  (acc[] read)")
    print("              ▼                    ▼")
    print("      ┌───────────────────────────────────┐")
    print("      │   REDUCE half  (1024 B input)     │")
    print("      └───────────────────────────────────┘")
    print("                       │")
    print("                       ▼  K partials")
    print("              ┌────────────────┐")
    print("              │   ACC_SPILL    │  K · E_out bytes")
    print("              └────────────────┘")
    print("                       │")
    print("                       ▼  K seeds")
    print("      ┌───────────────────────────────────┐")
    print("      │   BROADCAST half (1024 B output)  │")
    print("      └───────────────────────────────────┘")
    print("                       │")
    print("                       ▼ 512 B/cy  (RMW write back)")
    print("                  RFS_B (4 KB)")
    print()
    print("    REDUCE and BROADCAST halves share the same physical lanes ;")
    print("    a single beat uses EITHER reduce OR broadcast direction, not")
    print("    both (8 cy reduce, 1 cy spill, 8 cy broadcast = 17 cy total).")
    print()
    print("  ── NOTE on \"Benes-like\" terminology ──")
    print("    The shrink-then-expand SHAPE resembles Benes, but our tree does")
    print("    NOT use Benes' defining feature (per-stage 2×2 routing switches).")
    print("    See `--benes-analysis` for why this is essentially a fixed-wiring")
    print("    binary tree, NOT a Benes network — and why dropping the Benes")
    print("    framing removes ~10-20 % area and clarifies the design.")
    print()


# ---------------------------------------------------------------------------
# Benes-vs-binary-tree analysis
# ---------------------------------------------------------------------------

def print_benes_analysis() -> None:
    print(hbar())
    print("  IS THE BENES NETWORK ACTUALLY USEFUL?  CAN WE ELIMINATE IT?")
    print(hbar())
    print()
    print("  Short answer : YES, eliminate it.")
    print()
    print("  The \"Benes-like\" label has been a pedagogical conceit (the back-to-")
    print("  back butterfly SHAPE looks like Benes). In practice, our tree NEVER")
    print("  uses Benes' defining feature : arbitrary permutation routing via")
    print("  per-stage 2×2 switches. The connectivity is hardwired binary-tree")
    print("  fan-in / fan-out.")
    print()
    print("  ─── What pure Benes provides vs what we use ───────────────────────────")
    print()
    print("    feature                        Pure Benes(N)    Our tree")
    print("    ─────────────────────────────  ───────────────  ──────────────────────")
    print("    stages                         2·log2(N) − 1    2·log2(N)")
    print("    per-stage element              2×2 routing      ALU (reduce side)")
    print("                                   switch           FANOUT (bcast side)")
    print("                                   (4-in / 2-out)   (no routing)")
    print("    routing config / cycle         N · log2(N) b    0")
    print("    arbitrary permutation          YES              NO (binary tree only)")
    print("    sum / max / min reduce         NO (no ALU)      YES")
    print("    broadcast / fan-out            NO (route only)  YES")
    print("    ROW_SUM_EXP                    NO               YES")
    print("    in-tree sort                   partial          NO (need separate")
    print("                                   (with added                block)")
    print("                                   CMP_SWAP)")
    print()
    print("  ─── What our workload actually needs ─────────────────────────────────")
    print()
    print("    capability             needed?  Benes helps?")
    print("    ─────────────────────  ───────  ──────────────────────────────────")
    print("    reduce sum / max / min   YES    no  (binary tree is sufficient)")
    print("    broadcast / column exp   YES    no  (binary fan-out is sufficient)")
    print("    ROW_SUM_EXP              YES    no")
    print("    arbitrary permute        NO     —")
    print("    transpose                NO     done by TRegFile diagonal skew (§4)")
    print("    sort / merge_sort        maybe  no — bitonic uses FIXED butterfly,")
    print("                                    not Benes' configurable routing")
    print("    gather / scatter         NO     mask + ALU handle this")
    print("    FFT butterfly            NO     FFT also uses FIXED butterflies")
    print()
    print("  ─── Why Benes does NOT help with sort either ─────────────────────────")
    print()
    print("    Bitonic sort is a SEPARATE network class :")
    print("      - log²(N) / 2 stages, each with N/2 CMP_SWAP cells")
    print("      - per-stage wiring is FIXED (stride-1, stride-2, stride-4, ...")
    print("        butterfly patterns — same as FFT)")
    print("      - each cell does compare-and-conditional-swap (NOT pure routing)")
    print()
    print("    Pure Benes provides per-stage 2×2 ROUTING switches without ALU ;")
    print("    bitonic sort needs CMP_SWAP cells (= ALU + fixed wiring) at")
    print("    specific butterfly positions. These are DIFFERENT networks ;")
    print("    one does not subsume the other.")
    print()
    print("    Bitonic sort cell counts (rough, for context) :")
    print("      N =  128 (FP64) :  ~3.1 K CMP_SWAP cells")
    print("      N =  256 (FP32) :  ~4.0 K")
    print("      N =  512 (FP16) :  ~5.1 K")
    print("      N = 1024 (FP8 ) :  ~6.4 K")
    print()
    print("    A separate bitonic block can SHARE the CMP_SWAP cell library with")
    print("    the REDUCE tree (same 2-in / 1-out CMP_SWAP unit), but its wiring")
    print("    is its own butterfly, NOT a Benes routing fabric.")
    print()
    print("  ─── Recommended design AFTER dropping Benes routing ──────────────────")
    print()
    print("       Operand-A bus       Operand-B / Acc bus")
    print("            │                       │")
    print("            ▼                       ▼")
    print("   ┌──────────────────────────────────────────┐")
    print("   │  REDUCE  half  (binary fan-in tree)      │")
    print("   │   log2(N) stages, FIXED hardwired tree   │")
    print("   │   per-stage : N/2 ALU cells              │")
    print("   │   + INJECT  MUX  (format-tier truncate)  │")
    print("   │   + STOP-BIT MUX (row-width truncate)    │")
    print("   └────────────────┬─────────────────────────┘")
    print("                    ▼")
    print("          ┌──────────────────────┐")
    print("          │   ACC_SPILL latch    │")
    print("          └──────────────────────┘")
    print("                    ▼")
    print("   ┌──────────────────────────────────────────┐")
    print("   │  BROADCAST half (binary fan-out tree)    │")
    print("   │   log2(N) stages, FIXED hardwired tree   │")
    print("   │   per-stage : N/2 FANOUT cells           │")
    print("   │   + INJECT  MUX  (format-tier mirror)    │")
    print("   │   + STOP-BIT MUX (row-width mirror)      │")
    print("   └──────────────────────────────────────────┘")
    print()
    print("   *** OPTIONAL :  separate small bitonic-sort block,                ***")
    print("   ***             sharing the CMP_SWAP cell LIBRARY with REDUCE,    ***")
    print("   ***             but with its own FIXED butterfly wiring.          ***")
    print()
    print("  ─── Savings from eliminating Benes routing ───────────────────────────")
    print()
    print("    item                                          saving")
    print("    ────────────────────────────────────────────  ──────────────────────")
    print("    per-stage 2×2 routing MUXes                   ~ 100 K gates")
    print("    (~10 gates / byte × 1024 B × 10 stages)       ≈ 10–20 % of tree area")
    print("    critical-path MUX delay per stage             ~ 1–2 gate delays")
    print("                                                  per stage → integrated")
    print("                                                  tree latency drops")
    print("                                                  ~ 10–20 %")
    print("    microcode width per beat                      − 100 to − 200 b")
    print("                                                  (no routing-config")
    print("                                                  field needed)")
    print("    routing-config storage                        − ~ 2 KB latch / SRAM")
    print("    verification complexity                       1 order of magnitude")
    print("                                                  simpler (perm networks")
    print("                                                  are HARD to formally")
    print("                                                  verify ; binary trees")
    print("                                                  are trivial)")
    print("    conceptual clarity                            \"back-to-back butterfly")
    print("                                                  binary tree\" ≫")
    print("                                                  \"Benes-like\"")
    print()
    print("  ─── What we LOSE by eliminating Benes ────────────────────────────────")
    print()
    print("    Only one capability : in-tree arbitrary permutation. Our workload :")
    print("      • does not need it for compute  (reduce / broadcast / FMA /")
    print("        ROW_SUM_EXP)")
    print("      • handles transpose via TRegFile-4K diagonal skew  (vector4k_v2 §4)")
    print("      • handles sort via separate bitonic block          (cleaner anyway)")
    print("      • handles gather / scatter via mask + ALU          (rare in tile)")
    print("      • handles FFT via fixed-butterfly network          (not Benes)")
    print()
    print("    NET : pure simplification.  Strongly recommended.")
    print()
    print("  ─── Cleaner terminology ──────────────────────────────────────────────")
    print()
    print("    BEFORE :  \"Benes-like shrink-then-expand network\"")
    print("    AFTER  :  \"back-to-back butterfly binary tree\"")
    print("              or  \"binary fan-in / fan-out tree\"")
    print()
    print("    Same structure, but the new name correctly conveys :")
    print("      - it's a TREE (not a permutation network)")
    print("      - it has FIXED wiring (not configurable routing)")
    print("      - every internal node DOES COMPUTE (not just routes data)")
    print()


# ---------------------------------------------------------------------------
# Operand layout : parallel vs interleaved on the 1024 B tree input bus
# ---------------------------------------------------------------------------

def print_operand_layout() -> None:
    print(hbar())
    print("  OPERAND-A vs ACCUMULATOR PATH ON THE 1024 B TREE INPUT BUS")
    print("  (PARALLEL  vs  INTERLEAVED  ?)")
    print(hbar())
    print()
    print("  ╔══════════════════════════════════════════════════════════════════╗")
    print("  ║  REVISION NOTE :                                                  ║")
    print("  ║  This section assumes the REDUCE tree has a 1024 B input bus     ║")
    print("  ║  carrying both operand A and operand B.  In the FINAL design     ║")
    print("  ║  (--unary-reduce), REDUCE-class instructions are unary and the   ║")
    print("  ║  tree input shrinks to 512 B (operand A only).  This whole       ║")
    print("  ║  parallel-vs-interleaved question is then MOOT.                  ║")
    print("  ║  See `python3 vector32_v2.py --unary-reduce`.                    ║")
    print("  ╚══════════════════════════════════════════════════════════════════╝")
    print()
    print("  Recommendation : PARALLEL `[A | B]`,  AND the accumulator feedback")
    print("                   does NOT live on the 1024 B input bus at all.")
    print()
    print("  ─── Three candidate layouts ───────────────────────────────────────────")
    print()
    print("    layout                  upper 512 B      L0 cell pairing rule")
    print("    ──────────────────────  ───────────────  ────────────────────────────")
    print("    PARALLEL  [A | B]       operand B / acc  cell k pairs lane (2k, 2k+1)")
    print("                                              ⇒ same-side pair (intra-A")
    print("                                                or intra-B)")
    print("    INTERLEAVED [A0,B0,...] interleaved B    cell k pairs (A_k, B_k)")
    print("                                              ⇒ forced cross-AB pair")
    print("    A-only + small acc port idle             same as PARALLEL ; acc")
    print("                                              feeds via separate path")
    print()
    print("  ─── Why PARALLEL wins ─────────────────────────────────────────────────")
    print()
    print("    1) Accumulator is SMALL.  For ROW_SUM_EXP :")
    print("         acc_bytes = R · E_out")
    print("           R=8,  FP32 out : 32 B    R=32, FP32 out : 128 B")
    print("           R=64, FP64 out : 512 B   (worst common case)")
    print("       Forcing it into a 512 B half (interleaved) wastes ~80 % of")
    print("       the upper-bus bandwidth.")
    print()
    print("    2) RFS_A and RFS_B are physically separate 4 KB RFs.  Their")
    print("       512 B tilelets naturally feed adjacent halves of the 1024 B")
    print("       input bus — PARALLEL is the zero-cost layout.")
    print()
    print("    3) PARALLEL allows INDEPENDENT reduce of A or B.  INTERLEAVED")
    print("       forces every L0 cell to pair (A_k, B_k), which is wrong for")
    print("       single-tile reduce.")
    print()
    print("    4) Binary cross-AB ops (e.g. dot product = Σ A_k · B_k) are")
    print("       handled by the per-lane ALU array UPSTREAM of the tree")
    print("       (vector4k_v2.md §5 Stage-B core), NOT inside the tree's L0.")
    print("       The tree only sees the per-lane ALU's already-paired output.")
    print()
    print("    5) Accumulator feedback uses a SEPARATE small path :")
    print("       a K-wide RMW ALU sitting at the spill latch, NOT on the")
    print("       1024 B main input bus.")
    print()
    print("  ─── Recommended architecture ──────────────────────────────────────────")
    print()
    print("    [Operand-A tilelet 512 B]         [Operand-B tilelet 512 B  or idle]")
    print("              │                                       │")
    print("              ▼                                       ▼")
    print("       lanes 0..511                            lanes 512..1023")
    print("              │                                       │")
    print("              └────────────────┬──────────────────────┘")
    print("                               ▼")
    print("    ┌──────────────────────────────────────────────────────────┐")
    print("    │   1024 B REDUCE TREE  (back-to-back butterfly binary)    │")
    print("    │                                                          │")
    print("    │   L0 cell k pairs lanes (2k, 2k+1)  — DEFAULT same-side  │")
    print("    │     (intra-A in lower half,  intra-B in upper half)      │")
    print("    │                                                          │")
    print("    │   per-cell `pair_mode` bit can switch to cross-AB :      │")
    print("    │     cell k pairs lane k with lane k+512   (rarely used)  │")
    print("    │     cost : ~5 gates / cell × 512 cells = 2.5 K gates     │")
    print("    └────────────────┬─────────────────────────────────────────┘")
    print("                     │")
    print("                     ▼   K partials at chosen stop level")
    print("    ┌──────────────────────────────────────────┐")
    print("    │   K-wide  RMW ALU  +  ACC_SPILL latch    │  ← acc[K] from")
    print("    │   acc[r] += partial[r]  (in-place add)   │     RFS_B (small")
    print("    │   K · E_out bytes wide  (≤ 1 KB)         │     read, off the")
    print("    │                                          │     main bus)")
    print("    └────────────────┬─────────────────────────┘")
    print("                     │")
    print("                     ▼   acc[K] (small write back)")
    print("              RFS_B[acc_slot : acc_slot+K·E_out]")
    print()
    print("  ─── Why the acc path stays OFF the 1024 B bus ────────────────────────")
    print()
    print("    Putting the acc on the main bus would mean :")
    print("      • reading 512 B of RFS_B every beat just to use ~32 B of acc")
    print("      • routing acc through 9 stages of reduce tree to reach the K")
    print("        partials — but acc[r] is added at ONE place (the K-wide RMW)")
    print("        and doesn't need to traverse all 9 stages")
    print("      • burning power on 480 B of don't-care bus traffic per beat")
    print()
    print("    Keeping acc off the main bus (separate K-wide port + RMW ALU)")
    print("    saves :")
    print("      • ~80 % power on the upper-half bus (it can be clock-gated for")
    print("        unary reduce ops where operand B isn't used)")
    print("      • a wide cross-tree wire (acc would otherwise need to fan into")
    print("        whichever stop level holds the K partials)")
    print("      • clarifies the architecture : the tree REDUCES, the K-wide")
    print("        ALU ACCUMULATES — separation of concerns")
    print()
    print("  ─── When INTERLEAVED actually makes sense (rare) ──────────────────────")
    print()
    print("    Only in two niche cases :")
    print()
    print("      • DOT PRODUCT inside a single 1024 B tree pass :")
    print("          partial = Σ_k A_k · B_k  ,  reduce in one beat")
    print("        L0 cells doing (A_k · B_k) need (A_k, B_k) adjacent →")
    print("        INTERLEAVED helps. But our design routes the multiply through")
    print("        the per-lane ALU first ; the tree only sums afterwards.")
    print()
    print("      • SORTED-ARRAY MERGE (merge two sorted halves) where each L0")
    print("        cell does CMP_SWAP between A_k and B_k :")
    print("        same situation — the bitonic merge stage's wiring is what")
    print("        does this, NOT the reduce tree.")
    print()
    print("    Conclusion : INTERLEAVED's niche is covered by separate hardware")
    print("    (per-lane ALU upstream OR bitonic sort block). The reduce tree")
    print("    itself stays PARALLEL.")
    print()


# ---------------------------------------------------------------------------
# Accumulator register design : sharing, feedback path, and worst-case sizing
# ---------------------------------------------------------------------------

def print_accumulator_design() -> None:
    print(hbar())
    print("  ACCUMULATOR FLIP-FLOPS  (3 questions)")
    print("    Q1 : share with operand-B staging RF, or dedicated FFs ?")
    print("    Q2 : feedback path  ── local at the RMW, or back to tree front ?")
    print("    Q3 : worst-case (shape × format) acc byte requirement ?")
    print(hbar())
    print()
    print("  ╔══════════════════════════════════════════════════════════════════╗")
    print("  ║  REVISION NOTE :                                                  ║")
    print("  ║  Q1's recommendation (SEPARATE acc FFs) is SUPERSEDED by the      ║")
    print("  ║  --unary-reduce design, which makes REDUCE instructions unary,    ║")
    print("  ║  resolves the port conflicts at the instruction-class level, and  ║")
    print("  ║  reuses RFS_B as the acc.  Q2 (LOCAL feedback) and Q3 (sizing     ║")
    print("  ║  formulas) remain valid.                                          ║")
    print("  ║  See `python3 vector32_v2.py --unary-reduce` for the FINAL        ║")
    print("  ║  recommendation.                                                  ║")
    print("  ╚══════════════════════════════════════════════════════════════════╝")
    print()
    # ===== Q1 =====
    print("  ━━━ Q1 :  SEPARATE  vs  SHARE-with-RFS_B  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
    print()
    print("  Recommendation : SEPARATE, dedicated small acc-FF bank.")
    print("                   Do NOT repurpose RFS_B as accumulator.")
    print()
    print("  ─── Comparison ────────────────────────────────────────────────────────")
    print()
    print("    issue                       SHARED  (reuse RFS_B)        SEPARATE  (dedicated)")
    print("    ─────────────────────────   ───────────────────────────  ─────────────────────────")
    print("    Read-port conflict          RFS_B can't serve binary-    acc has its own port,")
    print("                                op operand B AND RMW         no contention")
    print("                                acc-fetch in same cycle")
    print()
    print("    3-src FMA  (D = A·B + C)    if C-operand routes from     acc independent →")
    print("                                acc, RFS_B port is busy →    same-cycle acc read +")
    print("                                stall                         RFS_B read possible")
    print()
    print("    Op-to-op pipelining         op_n's acc result lives in   acc decoupled ; op_{n+1}")
    print("                                RFS_B ; op_{n+1} that wants  is free to use RFS_B")
    print("                                RFS_B as operand B must")
    print("                                first move it out → bubble")
    print()
    print("    Acc footprint               steals up to 1 KB of RFS_B   right-sized (2 KB max,")
    print("                                even though typical acc      see Q3) ; no waste in")
    print("                                < 512 B → wastes RFS_B       RFS_B")
    print()
    print("    Verification surface        one RF, two semantics ;      separation of concerns ;")
    print("                                bugs cross-pollute           clean boundaries")
    print()
    print("    Area                        −16 K FFs (saved)            +16 K FFs (~50 K gates")
    print("                                                              in 28 nm) for 2 KB acc")
    print()
    print("  Trade-off : we PAY ~50 K gates to GAIN port-conflict-free pipelining,")
    print("  3-src-FMA support, and a clean RFS_B that is ALWAYS the operand-B")
    print("  staging register. The 50 K gates is < 0.05 % of the VEC die, well")
    print("  worth the architectural simplification.")
    print()
    # ===== Q2 =====
    print("  ━━━ Q2 :  Feedback path ── LOCAL or back to TREE FRONT ?  ━━━━━━━━━━━━")
    print()
    print("  Recommendation : LOCAL feedback only.  acc never goes back to L0 of")
    print("                   the reduce tree.")
    print()
    print("  ─── Comparison ────────────────────────────────────────────────────────")
    print()
    print("    path                LOCAL feedback              FRONT feedback")
    print("                        (recommended)               (rejected)")
    print("    ─────────────────   ─────────────────────────   ─────────────────────────")
    print("    where it loops      tree-exit → K-wide RMW      tree-exit → 4 KB bus →")
    print("                        ALU → acc FF → RMW          back to tree L0 input")
    print("                                                    (upper 512 B half)")
    print()
    print("    operations that     reduce (sum/max/min)        per-element accumulation")
    print("    benefit             ROW_SUM_EXP                 (e.g. dense GEMM tile-acc)")
    print("                        COL_SUM_EXP                 — but those use the per-lane")
    print("                        ROW_MAX, COL_MIN, ...       FMA + RFA_A/B already !")
    print()
    print("    cost                K · E_out wide ALU +        4 KB feedback bus + steals")
    print("                        1 short feedback wire       upper 512 B input port +")
    print("                                                    every beat goes through 9")
    print("                                                    extra tree levels for what")
    print("                                                    is a single ADD")
    print()
    print("    gates               ~ 1 K                       ~50 K + 1 extra pipe stage")
    print()
    print("  Why : in ROW_SUM_EXP the acc is combined with the new partials ONLY")
    print("  ONCE per beat (K-wide ADD).  Routing acc back through 9 reduce-tree")
    print("  levels just to add it would be wasted travel.  Per-element GEMM-class")
    print("  accumulation belongs in the per-lane FMA core (vector4k_v2.md §5.5),")
    print("  not in the reduce-tree pipeline.")
    print()
    print("  ─── Architecture diagram (LOCAL feedback) ─────────────────────────────")
    print()
    print("             ┌────────────────────────────────────┐")
    print("             │   1024 B REDUCE TREE                │")
    print("             │   (operand A on lower half,         │")
    print("             │    operand B / idle on upper half)  │")
    print("             └─────────────────┬───────────────────┘")
    print("                               │  K · E_out  partials")
    print("                               ▼")
    print("    ┌──────────────────────────────────────────────┐")
    print("    │  K-WIDE  RMW  ALU  (ADD / MAX / MIN)         │")
    print("    │                                              │")
    print("    │       acc_FF[r] := acc_FF[r]  ⊕  partial[r]  │ ◀──┐")
    print("    └──────────────────────┬───────────────────────┘    │ same-cycle")
    print("                           │                            │ feedback")
    print("                           ▼                            │")
    print("            ┌───────────────────────────────────┐       │")
    print("            │   ACC FF bank   (2 KB max)         │ ──────┘")
    print("            │   addressed by acc_slot[r]         │")
    print("            └─────────────────┬─────────────────┘")
    print("                              │  on broadcast phase :")
    print("                              ▼")
    print("            ┌───────────────────────────────────┐")
    print("            │   ACC_SPILL latch (snapshot)       │")
    print("            │   feeds BROADCAST tree input       │")
    print("            └───────────────────────────────────┘")
    print()
    # ===== Q3 =====
    print("  ━━━ Q3 :  Worst-case  acc-byte  requirement  ━━━━━━━━━━━━━━━━━━━━━━━━━")
    print()
    print("  ROW_SUM_EXP acc formula :")
    print()
    print("      acc_bytes = R · E_out                    (one slot per row)")
    print("                = (4096 / (C · E_in)) · E_out  (using R·C·E_in = 4096)")
    print()
    print("      Same-precision out  (E_out = E_in)  :   acc_bytes = 4096 / C")
    print("      Double-precision out (E_out = 2·E_in) : acc_bytes = 8192 / C")
    print()
    print("  Note : acc size depends only on C (reduce dim) and double-width flag.")
    print("         It is INDEPENDENT of E_in.")
    print()
    print("  ─── ROW_SUM_EXP : acc as a function of minimum supported C ────────────")
    print()
    print("    C_min   same-prec acc_max   double-prec acc_max   remark")
    print("    ─────   ─────────────────   ───────────────────   ─────────────────────────")
    print("        2          2048 B              4096 B          degenerate (sum of 2)")
    print("        4          1024 B              2048 B          short reduce")
    print("        8           512 B              1024 B          ← reasonable lower bound")
    print("       16           256 B               512 B          common min")
    print("       64            64 B               128 B          typical head-dim")
    print("      256            16 B                32 B          large head")
    print()
    print("  ─── Other reduce-class ops (worst case at 4 KB tile) ──────────────────")
    print()
    print("    operation                   acc formula                worst-case")
    print("    ─────────────────────────   ────────────────────────   ──────────")
    print("    ROW_SUM_EXP  / ROW_MAX      R · E_out                  ~2 KB")
    print("    COL_SUM_EXP  / COL_MAX      C · E_out                  ~2 KB")
    print("    FULL_REDUCE (whole tile)    E_out                      8 B")
    print("    ARGMAX per row              R · (E_out + idx_bytes)    ~2.5 KB")
    print("    TOP-K per row (K=8)         R · K · E_out             ⚠  ~16 KB")
    print()
    print("    ⚠ TOP-K is an outlier — it should NOT share the reduce-tree acc.")
    print("       Use a dedicated K-element scratch attached to the SORT network.")
    print("       Otherwise the reduce-tree acc would be 8× over-provisioned.")
    print()
    print("  ─── Sizing recommendation ─────────────────────────────────────────────")
    print()
    print("    target                                       acc-FF size")
    print("    ──────────────────────────────────────────   ───────────")
    print("    cheap : same-prec only,  C ≥ 8                 1 KB")
    print("    standard : double-prec,  C ≥ 8                 1 KB  (out_max = 1024)")
    print("    standard : double-prec,  C ≥ 4                 2 KB  ← RECOMMENDED")
    print("    permissive : double-prec, C ≥ 2                4 KB")
    print()
    print("    RECOMMENDED : 2 KB (16 K FF, ~50 K gates in 28 nm)")
    print("      ✓ covers ROW_SUM_EXP and COL_SUM_EXP up to R, C ≤ 256 in")
    print("        double-precision mode")
    print("      ✓ covers ARGMAX per row up to R = 256")
    print("      ✓ leaves headroom for narrower C (down to C = 4 in double-prec)")
    print("      ✗ does NOT try to satisfy TOP-K (handled separately)")
    print("      ✗ does NOT try to satisfy degenerate C = 2 (cheap to outlaw")
    print("        in the spec — there's no real workload)")
    print()
    print("  ─── Cost-of-doubling table (for design space exploration) ─────────────")
    print()
    print("    acc size   FFs (×16 b)   cell area (28 nm)   coverage gained")
    print("    ────────   ───────────   ─────────────────   ─────────────────────────")
    print("    1 KB       8192          ~25 K gates         standard up to C ≥ 16")
    print("    2 KB       16384         ~50 K gates         RECOMMENDED  ← best knee")
    print("    4 KB       32768         ~100 K gates        covers C = 2 (rare)")
    print("    8 KB       65536         ~200 K gates        no further coverage benefit")
    print()


# ---------------------------------------------------------------------------
# FINAL : full-system block diagram with all revisions applied
# ---------------------------------------------------------------------------

def print_final_block_diagram() -> None:
    print(hbar())
    print("  FINAL  VEC-4K-v2  BLOCK DIAGRAM")
    print("  All revisions applied : unary REDUCE + RFS_B-as-acc + 512 B half tree")
    print("                          + time-muxed sort + simplified broadcast")
    print(hbar())
    print()
    print("  ─── Top-level datapath ────────────────────────────────────────────────")
    print()
    print("    INSTRUCTION DISPATCH")
    print("           │")
    print("           ▼")
    print("    ┌──────────────────────────────────────────────────────────────────┐")
    print("    │                  INSTRUCTION CLASS DECODE                         │")
    print("    │   binary │ unary REDUCE │ unary SORT │ FMA(3-src) │ MOV/COPY      │")
    print("    └──────┬─────────┬──────────┬───────────┬──────────────┬───────────┘")
    print("           │         │          │           │              │")
    print("    ╔══════╪═════════╪══════════╪═══════════╪══════════════╪═══════════╗")
    print("    ║      ▼         ▼          ▼           ▼              ▼           ║")
    print("    ║  ┌─────────────────────────────────────────────────────────┐     ║")
    print("    ║  │           STORAGE  (4 KB each, total 16 KB)              │     ║")
    print("    ║  │                                                         │     ║")
    print("    ║  │  RFS_A  (4 KB)        RFS_B  (4 KB, DUAL ROLE)          │     ║")
    print("    ║  │  ── operand A         ── operand B (binary class)       │     ║")
    print("    ║  │  ── sort scratch      ── operand C (3-src FMA)          │     ║")
    print("    ║  │                       ── ACC + broadcast output         │     ║")
    print("    ║  │                          (REDUCE class)                 │     ║")
    print("    ║  │                                                         │     ║")
    print("    ║  │  RFA_A  (4 KB)        RFA_B  (4 KB)                     │     ║")
    print("    ║  │  ── per-lane FMA acc  ── per-lane FMA acc (ping-pong)   │     ║")
    print("    ║  └────┬────────┬─────────────────┬─────────────────────────┘     ║")
    print("    ║       │        │                 │                                ║")
    print("    ║       │512 B   │512 B            │512 B                           ║")
    print("    ║       ▼        ▼                 ▼                                ║")
    print("    ║                                                                  ║")
    print("    ║                            BINARY-CLASS DISPATCH                  ║")
    print("    ║      (instruction routed to EXACTLY ONE of the 5 paths below)     ║")
    print("    ║                                                                  ║")
    print("    ║       │              │              │              │              ║")
    print("    ║       ▼              ▼              ▼              ▼              ║")
    print("    ║  ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐           ║")
    print("    ║  │ PER-LANE│   │ PER-LANE│   │ 16-COPY  │   │ MOV /    │           ║")
    print("    ║  │ FMA     │   │ ELEM-   │   │ GENERAL  │   │ COPY /   │           ║")
    print("    ║  │ ARRAY   │   │ WISE    │   │ SF       │   │ MASK     │           ║")
    print("    ║  │         │   │ ALU     │   │ ENGINE   │   │ APPLY    │           ║")
    print("    ║  │         │   │ ARRAY   │   │ (FP16+32)│   │          │           ║")
    print("    ║  ├─────────┤   ├─────────┤   ├─────────┤   ├─────────┤           ║")
    print("    ║  │ ops:    │   │ ops:    │   │ ops:    │   │ ops:    │           ║")
    print("    ║  │ FMUL    │   │ AND/OR/ │   │ sigmoid │   │ MOV     │           ║")
    print("    ║  │ FADD    │   │ XOR/NOT │   │ tanh    │   │ LOAD    │           ║")
    print("    ║  │ FMA     │   │ ==,!=,  │   │ log     │   │ STORE   │           ║")
    print("    ║  │ FNMA    │   │ <,>,<=, │   │ sqrt    │   │ MASK    │           ║")
    print("    ║  │ A·B+C   │   │ >=      │   │ recip   │   │         │           ║")
    print("    ║  │ (3-src) │   │ MIN/MAX │   │ rsqrt   │   │         │           ║")
    print("    ║  │ VLERP   │   │ ABS/NEG │   │ (NO exp │   │         │           ║")
    print("    ║  │         │   │ /SIGN   │   │  here)  │   │         │           ║")
    print("    ║  │         │   │ shift   │   │         │   │         │           ║")
    print("    ║  │         │   │ clamp   │   │         │   │         │           ║")
    print("    ║  │         │   │ sat     │   │         │   │         │           ║")
    print("    ║  ├─────────┤   ├─────────┤   ├─────────┤   ├─────────┤           ║")
    print("    ║  │ 128 ×   │   │ 128 ×   │   │ 16 units│   │ DMA-    │           ║")
    print("    ║  │ 8 B FMA │   │ ~700 g  │   │ each    │   │ style   │           ║")
    print("    ║  │ lanes   │   │ /lane   │   │ muxes   │   │         │           ║")
    print("    ║  │         │   │         │   │ 8 lanes │   │         │           ║")
    print("    ║  │         │   │         │   │ ~ 8 cy  │   │         │           ║")
    print("    ║  │         │   │         │   │ /tile   │   │         │           ║")
    print("    ║  ├─────────┤   ├─────────┤   ├─────────┤   ├─────────┤           ║")
    print("    ║  │ ~1.28 M │   │ ~90 K   │   │ ~215 K  │   │ <1 K    │           ║")
    print("    ║  │ gates   │   │ gates   │   │ gates   │   │ gates   │           ║")
    print("    ║  │         │   │         │   │ (LUT    │   │         │           ║")
    print("    ║  │         │   │         │   │ shared) │   │         │           ║")
    print("    ║  └────┬────┘   └────┬────┘   └────┬────┘   └────┬────┘           ║")
    print("    ║       │             │             │             │                 ║")
    print("    ║       │             │             │             │                 ║")
    print("    ║                                                                  ║")
    print("    ║                  ┌──────────────────────────────┐                ║")
    print("    ║                  │  64-COPY DEDICATED EXP UNIT   │ ← softmax-fused║")
    print("    ║                  │  (FP16 4-th order minimax)    │   exp routed   ║")
    print("    ║                  │  range-reduction + 4-coeff    │   here only.   ║")
    print("    ║                  │  poly + scale (k-bit shift)   │                ║")
    print("    ║                  │  ~5 K g/copy × 64 = 320 K g   │                ║")
    print("    ║                  │  64 elem/cy → 4 cy/tile (FP16)│                ║")
    print("    ║                  │  matches cube GEMM throughput │                ║")
    print("    ║                  │  for d_h ≥ 64 attention       │                ║")
    print("    ║                  └────────────┬─────────────────┘                 ║")
    print("    ║                               │                                  ║")
    print("    ║       └──────┬──────┴────┬────┴─────┬──────┴──────┐               ║")
    print("    ║              │           │          │             │               ║")
    print("    ║              ▼           ▼          ▼             ▼               ║")
    print("    ║       to  RFS_B  /  RFA_A  /  RFA_B    (per-instruction dest)     ║")
    print("    ║                                                                  ║")
    print("    ║       (the 5 BINARY paths share RFS_A and RFS_B read ports —     ║")
    print("    ║        only 1 path active per cycle, no port contention)         ║")
    print("    ║                                                                  ║")
    print("    ║  ───────────────────────────────────────────────────────────     ║")
    print("    ║                                                                  ║")
    print("    ║                            UNARY-REDUCE-CLASS DISPATCH            ║")
    print("    ║         (single-source A from RFS_A,  acc/out lives in RFS_B)     ║")
    print("    ║                                                                  ║")
    print("    ║                                                                  ║")
    print("    ║  ┌──────────────────────────────┐                                 ║")
    print("    ║  │  REDUCE TREE   (512 B input)  │  ← unary class only            ║")
    print("    ║  │  9 levels × ~250 gates/cell-B │     reads 1 × 512 B from RFS_A ║")
    print("    ║  │  with format-tier INJECT MUX  │     produces K partials        ║")
    print("    ║  │  + stop-bit BOTTOM MUX        │     ~175 K gates               ║")
    print("    ║  └────────────┬─────────────────┘                                 ║")
    print("    ║               │ K · E_out partials at chosen stop level           ║")
    print("    ║               ▼                                                   ║")
    print("    ║  ┌──────────────────────────────┐                                 ║")
    print("    ║  │  K-WIDE RMW ALU + LOCAL FB    │ ◀─── acc[r] from RFS_B         ║")
    print("    ║  │  acc[r] := acc[r] ⊕ part[r]   │      (small read, ≤ 2 KB)      ║")
    print("    ║  │  ~30 K gates  + same-cy fb    │                                ║")
    print("    ║  └────────────┬─────────────────┘                                 ║")
    print("    ║               │ updated acc[]                                     ║")
    print("    ║               ▼                                                   ║")
    print("    ║          RFS_B  (in-place RMW, 'acc role')                       ║")
    print("    ║                                                                  ║")
    print("    ║  ───────── (broadcast phase, after reduce ends) ─────────         ║")
    print("    ║                                                                  ║")
    print("    ║  ┌──────────────────────────────┐                                 ║")
    print("    ║  │  ACC_SPILL latch (~64 B)      │ ← final acc snapshot           ║")
    print("    ║  │  reverse-order broadcast trick│   (avoids RAW with overwrite)  ║")
    print("    ║  └────────────┬─────────────────┘                                 ║")
    print("    ║               │ K acc seeds                                       ║")
    print("    ║               ▼                                                   ║")
    print("    ║  ┌──────────────────────────────┐                                 ║")
    print("    ║  │  BROADCAST TREE  (1 → 512 B)  │  ← 9 levels of FANOUT cells    ║")
    print("    ║  │  ~24 gate/cell × 511 cells    │     selects sub-tree per row   ║")
    print("    ║  │  ~60 K gates  (3× smaller     │     no ALU, just wire fan-out  ║")
    print("    ║  │   than REDUCE tree)           │     + buffer + stop-bit MUX    ║")
    print("    ║  └────────────┬─────────────────┘                                 ║")
    print("    ║               │ 512 B / beat                                      ║")
    print("    ║               ▼                                                   ║")
    print("    ║          RFS_B (overwritten in REVERSE row order)                 ║")
    print("    ║                                                                  ║")
    print("    ║  ───────────────────────────────────────────────────────────     ║")
    print("    ║                                                                  ║")
    print("    ║  ┌──────────────────────────────┐                                 ║")
    print("    ║  │  TIME-MUXED SORT NETWORK      │  ← unary SORT class            ║")
    print("    ║  │                                │     reads from RFS_A,          ║")
    print("    ║  │  ┌─────────────────────────┐  │     writes back RFS_A          ║")
    print("    ║  │  │ PERMUTE  (10-lvl bfly)   │  │     in 55 cy                   ║")
    print("    ║  │  │ stride d ∈ {1..512}      │  │                                ║")
    print("    ║  │  │ ~50 K gates              │  │                                ║")
    print("    ║  │  └────────┬────────────────┘  │                                ║")
    print("    ║  │           ▼                   │                                ║")
    print("    ║  │  ┌─────────────────────────┐  │                                ║")
    print("    ║  │  │ 1 STAGE × 512 CMP_SWAP   │  │                                ║")
    print("    ║  │  │ + per-cell asc/desc bit  │  │                                ║")
    print("    ║  │  │ ~25 K gates              │  │                                ║")
    print("    ║  │  └────────┬────────────────┘  │                                ║")
    print("    ║  │           ▼                   │                                ║")
    print("    ║  │  ┌─────────────────────────┐  │                                ║")
    print("    ║  │  │ SCHEDULE ROM + FSM       │  │                                ║")
    print("    ║  │  │ 660 b ROM + ~10 K g      │  │                                ║")
    print("    ║  │  └─────────────────────────┘  │                                ║")
    print("    ║  │                                │                                ║")
    print("    ║  │  total ~85 K gates             │                                ║")
    print("    ║  │  (vs 1.4 M full pipeline)      │                                ║")
    print("    ║  └────────────┬─────────────────┘                                 ║")
    print("    ║               │ sorted result (after 55 cy)                       ║")
    print("    ║               ▼                                                   ║")
    print("    ║          RFS_A (in place)                                        ║")
    print("    ║                                                                  ║")
    print("    ╚══════════════════════════════════════════════════════════════════╝")
    print()
    print("  ─── Storage  ──────────────────────────────────────────────────────────")
    print()
    print("    name      size    role(s)")
    print("    ──────    ─────   ──────────────────────────────────────────────────")
    print("    RFS_A     4 KB    operand A staging  +  sort scratch")
    print("    RFS_B     4 KB    operand B staging  (binary class)")
    print("                       operand C staging  (3-src FMA)")
    print("                       accumulator + broadcast output (REDUCE class)")
    print("    RFA_A     4 KB    per-lane FMA accumulator  (ping)")
    print("    RFA_B     4 KB    per-lane FMA accumulator  (pong)")
    print("    ────────────────────────────────────────────────────────────────────")
    print("    TOTAL    16 KB    storage  (32 K bytes × 8 b = 128 K FFs ≈ 320 K g)")
    print()
    print("    Plus:  ACC_SPILL latch  ~64 B    (~1.5 K gates)")
    print("           Stage-Sort scratch  uses RFS_A  (no extra)")
    print()
    print("  ─── Compute blocks  ─────────────────────────────────────────────────")
    print()
    print("    block                              gates    notes")
    print("    ──────────────────────────────     ──────   ────────────────────────")
    print("    Per-lane FMA array                 1.28 M   128 lanes × 8 B FMA")
    print("    Per-lane element-wise ALU            90 K   128 × ~700 g (logical,")
    print("                                                compare, min/max, abs,")
    print("                                                neg, shift, clamp)")
    print("    16-copy general SF engine           215 K   16 SF units (8 lanes")
    print("       (no exp ; exp is dedicated)              each, 8 cy/tile),")
    print("                                                with shared LUT/seed")
    print("                                                tables (~55 K shared)")
    print("                                                + 16× datapath (~160 K)")
    print("    64-copy dedicated exp unit          320 K   64 exp units (2 lanes")
    print("       (FP16 4-th order minimax)                each, 4 cy/tile),")
    print("                                                each ~5 K g, FP16-only")
    print("                                                matches cube GEMM thrpt")
    print("    REDUCE tree (512 B, unary)          175 K   9 lvl, format-tier MUX,")
    print("                                                stop-bit MUX, ALU cells")
    print("    K-wide RMW ALU + local feedback      30 K   acc fold (≤ 2 KB wide)")
    print("    BROADCAST tree (1 → 512 B)           60 K   9 lvl, FANOUT only")
    print("                                                (no ALU per cell)")
    print("    SORT network (1-stage time-muxed)    85 K   permute 50 K + cells 25 K")
    print("                                                + sequencer 10 K")
    print("    ACC_SPILL latch + control             3 K")
    print("    Microcode sequencer (top-level)       8 K   reduce/sort/broadcast FSM")
    print("    ──────────────────────────────     ──────")
    print("    TOTAL compute (excl per-lane FMA)   986 K   adds element-wise ALU")
    print("                                                + 16-copy SF + 64-copy")
    print("                                                exp + reduce + sort +")
    print("                                                broadcast")
    print("    TOTAL incl per-lane FMA            2.27 M")
    print()
    print("  ─── REDUCE vs BROADCAST tree cost (the original question) ────────────")
    print()
    print("    REDUCE tree  N=512, 9 levels  (unary, half-width final design)")
    print()
    print("       level  cells  ALU bytes/cell   total byte-cells")
    print("       ─────  ─────  ──────────────   ────────────────")
    print("       L0      256        1                  256")
    print("       L1      128        2                  256")
    print("       L2       64        4                  256")
    print("       L3       32        8                  256")
    print("       L4       16        8                  128")
    print("       L5        8        8                   64")
    print("       L6        4        8                   32")
    print("       L7        2        8                   16")
    print("       L8        1        8                    8")
    print("       ────────────────────────────────────────────")
    print("       sum     511                          1272 byte-cells")
    print()
    print("       byte-cell ALU (ADD/CMP_SWAP_MAX_MIN)   ~30 g")
    print("       cell ALU subtotal                      1272 × 30 = ~38 K g")
    print("       format-tier INJECT MUX                          ~40 K g")
    print("       stop-bit BOTTOM MUX (9 stages)                  ~45 K g")
    print("       pipeline registers (9 stages)                   ~50 K g")
    print("       per-cell control + carry/index                   ~5 K g")
    print("       ──────────────────────────────────────────")
    print("       REDUCE tree TOTAL                              ~175 K g")
    print()
    print("    BROADCAST tree  1 → 512, 9 levels  (FANOUT only)")
    print()
    print("       level  cells  fanout    bytes wide")
    print("       ─────  ─────  ──────    ──────────")
    print("       L0        1   1 → 2     8 B")
    print("       L1        2   1 → 2     8 B")
    print("       ...     ...   1 → 2     8 B")
    print("       L8      256   1 → 2     8 B")
    print("       sum     511   simple wire fan-out + buffer (no ALU)")
    print()
    print("       per-cell  =  8 B × 3 g (buffer/repeater)        =  24 g")
    print("       cell subtotal                                   ~12 K g")
    print("       stop-bit MUX (sub-tree select)                  ~20 K g")
    print("       pipeline registers (small : K seeds)            ~25 K g")
    print("       per-cell control                                 ~3 K g")
    print("       ──────────────────────────────────────────")
    print("       BROADCAST tree TOTAL                            ~60 K g")
    print()
    print("    RATIO :  REDUCE / BROADCAST  =  175 / 60  ≈  2.9×")
    print()
    print("    BROADCAST is ~3× CHEAPER than REDUCE because :")
    print("      • cells are wire fan-out + buffer  (no ALU)        : 24 g vs 250 g")
    print("      • no format-tier injection MUX                      : skips ~40 K")
    print("      • no per-stage ALU width promotion                  : flat 8 B")
    print("      • no carry/index propagation                        : skips per-cell")
    print("      • smaller pipeline registers (K seeds vs full lane) : ~50 % smaller")
    print()
    print("    But BROADCAST is NOT 'far less' — same cell count and similar wiring.")
    print("    The factor of 3 is because each cell is much simpler, not because")
    print("    fewer cells.")
    print()
    print("  ─── Cumulative savings vs. naïve original (binary REDUCE + full sort) ─")
    print()
    print("    component                     naïve         FINAL          delta")
    print("    ───────────────────────────   ──────────    ──────────     ──────")
    print("    REDUCE tree (1024 → 512 B)      ~350 K        ~175 K       −175 K")
    print("    Dedicated acc FF bank            ~50 K         0 (RFS_B)    −50 K")
    print("    ACC_SPILL latch                   ~7 K         ~1.5 K       −5 K")
    print("    BROADCAST tree                  ~120 K         ~60 K       −60 K")
    print("    SORT network (full → muxed)    ~1.4 M         ~85 K     −1315 K")
    print("    L0 cross-AB MUX (now removed)    ~2.5 K        0           −2.5 K")
    print("    ─────────────────────────────────────────────────────────  ──────")
    print("    NET SAVINGS                                              ~−1.6 M g")
    print()
    print("    Total VEC-unit area BEFORE :  ~3.6 M gates  (binary REDUCE +")
    print("                                                  full-pipeline sort,")
    print("                                                  no elem-wise ALU,")
    print("                                                  no special-func)")
    print("    Subtotal after revisions  :  ~2.0 M gates  (revisions only)")
    print("    + element-wise ALU  (NEW) :  + 90 K gates")
    print("    + 16-copy general SF      :  +215 K gates   (16 lane-throughput,")
    print("                                                 LUT/seed shared,")
    print("                                                 no exp inside)")
    print("    + 64-copy dedicated exp   :  +320 K gates   (FP16 4-th minimax,")
    print("                                                 64 elem/cy = matches")
    print("                                                 cube GEMM throughput)")
    print("    Total VEC-unit area FINAL :  ~2.63 M gates  ≈  27 % SMALLER")
    print("                                                 (vs naïve baseline)")
    print()
    print("    Performance impact :")
    print("      • REDUCE / ROW_SUM_EXP  :  unchanged latency, unchanged throughput")
    print("      • SORT throughput drops to 1/55  (acceptable for DL workloads)")
    print("      • SORT latency unchanged (55 cy)")
    print("      • All other ops (per-lane FMA, GEMM acc) unchanged")
    print("      • + element-wise ALU enables : ReLU, GELU, Sigmoid, Tanh, SiLU,")
    print("        exp, log, sqrt, comparisons, logical ops, type conversion")
    print()
    print("  ─── TOTAL AREA COST TABLE  (28 nm equivalent gates, FINAL design) ────")
    print()
    print("    category   block                                  gates    % of VEC")
    print("    ────────   ────────────────────────────────────   ──────   ────────")
    print("    STORAGE    RFS_A   (4 KB DFF + R/W ports)           80 K     3.0 %")
    print("               RFS_B   (4 KB DFF, dual-role)            80 K     3.0 %")
    print("               RFA_A   (4 KB DFF, per-lane acc ping)    80 K     3.0 %")
    print("               RFA_B   (4 KB DFF, per-lane acc pong)    80 K     3.0 %")
    print("               ACC_SPILL latch  (~64 B + ctrl)          1.5 K    0.1 %")
    print("               Microcode ROM    (~4 KB instr table)     5 K      0.2 %")
    print("               Schedule ROMs    (sort + reduce stage)   1.5 K    0.1 %")
    print("               ──────────────────────────────────────────────────────")
    print("               STORAGE subtotal                       328 K     12.5 %")
    print()
    print("    COMPUTE    Per-lane FMA array  (128 lanes × 8B)  1280 K     48.7 %")
    print("    DATAPATH   Per-lane element-wise ALU array         90 K      3.4 %")
    print("                  ── logical (AND/OR/XOR/NOT)")
    print("                  ── compare (==, !=, <, >, <=, >=)")
    print("                  ── min/max element-wise + cmp_swap")
    print("                  ── abs / neg / sign")
    print("                  ── shift (LSL/LSR/ASR) + clamp/sat")
    print("                  ── 128 lanes × ~700 g/lane")
    print("               16-copy general SF engine              215 K      8.2 %")
    print("                  ── 16 SF units, each serves 8 lanes,")
    print("                     time-muxes over 8 cy per tile")
    print("                  ── handles : sigmoid, tanh, log,")
    print("                     sqrt, recip, rsqrt   (NO exp)")
    print("                  ── shared LUT/seed tables (banked):")
    print("                       sigmoid/tanh LUT          ~15 K shared")
    print("                       log piecewise + LUT       ~25 K shared")
    print("                       recip / rsqrt seed table  ~15 K shared")
    print("                  ── per-copy datapath (×16):")
    print("                       Newton-Raphson sub-engine  ~5 K × 16 = 80 K")
    print("                       polynomial+sequencer       ~5 K × 16 = 80 K")
    print("                  ── 16 lane-throughput, 8 cy/tile FP16")
    print("                  ── FP16 + FP32 mixed (could be FP16-only")
    print("                     to save ~50 K g)")
    print("               64-copy dedicated exp unit             320 K     12.2 %")
    print("                  ── 64 exp units, each serves 2 lanes,")
    print("                     time-muxes over 4 cy per tile")
    print("                  ── FP16 4-th order minimax polynomial")
    print("                     (max relative error ~0.1 %)")
    print("                  ── per-copy : ~5 K g (range red 0.5 K +")
    print("                     poly MAC 3.5 K + scale 0.5 K + seq 0.5 K)")
    print("                  ── 64 elem/cy → 4 cy/tile FP16, matches")
    print("                     cube GEMM throughput for d_h ≥ 64")
    print("                  ── used by: SOFTMAX, SOFTPLUS, exp(x),")
    print("                     fused softmax-exp microcode")
    print("                  ── alternative: i-BERT 2nd-order (~80 K total")
    print("                     for INT8 quant inference)")
    print("               REDUCE tree         (512 B unary)      175 K      6.6 %")
    print("               K-wide RMW ALU + local feedback         30 K      1.1 %")
    print("               BROADCAST tree      (1 → 512 B)         60 K      2.3 %")
    print("               SORT network        (1-stage muxed)     85 K      3.2 %")
    print("               ──────────────────────────────────────────────────────")
    print("               COMPUTE subtotal                      2255 K     85.7 %")
    print()
    print("    CONTROL    Top-level FSM / sequencer                8 K      0.3 %")
    print("               Instruction class decode + dispatch      5 K      0.2 %")
    print("               Format-tier / stop-bit / dir ctrl        3 K      0.1 %")
    print("               Mask / predicate / lane-enable          5 K      0.2 %")
    print("               ROB / scoreboard interface               5 K      0.2 %")
    print("               Misc glue (clock, scan, debug)          10 K      0.4 %")
    print("               ──────────────────────────────────────────────────────")
    print("               CONTROL subtotal                        36 K      1.4 %")
    print()
    print("    ════════════════════════════════════════════════════════════════")
    print("    TOTAL  VEC-4K-v2  (FINAL design, K=16+K=64)       ~2.63 M  100.0 %")
    print("    ════════════════════════════════════════════════════════════════")
    print()
    print("  ─── BEFORE vs AFTER side-by-side ──────────────────────────────────────")
    print()
    print("    block                              BEFORE      AFTER       delta")
    print("    ──────────────────────────────     ──────      ──────      ─────")
    print("    Per-lane FMA array                 1280 K      1280 K        0")
    print("    Per-lane element-wise ALU             0 K        90 K      +90 K  ★ NEW")
    print("    16-copy general SF (no exp)           0 K       215 K     +215 K  ★ NEW")
    print("    64-copy dedicated exp                 0 K       320 K     +320 K  ★ NEW")
    print("    REDUCE tree (1024B → 512B)          350 K       175 K     −175 K")
    print("    BROADCAST tree (simplified)         120 K        60 K      −60 K")
    print("    K-wide RMW ALU + local feedback      30 K        30 K        0")
    print("    Dedicated acc FF bank (→ RFS_B)      50 K         0 K      −50 K")
    print("    ACC_SPILL latch                       7 K         1.5 K     −5.5 K")
    print("    SORT network (full → 1-stage mux)  1400 K        85 K   −1315 K")
    print("    L0 cross-AB pair_mode MUX             2.5 K       0 K       −2.5 K")
    print("    Storage RFs (RFS_A/B + RFA_A/B)     320 K       320 K        0")
    print("    Microcode + schedule ROMs             5 K         6.5 K     +1.5 K")
    print("    Control / dispatch / glue            36 K        36 K        0")
    print("    ────────────────────────────────────────────────────────  ──────")
    print("    TOTAL  VEC-4K-v2                  ~3.6 M      ~2.63 M  −0.97 M g")
    print("                                                              (27 % saved)")
    print()
    print("    Note : ★ NEW blocks (element-wise ALU + 16-copy general SF +")
    print("           64-copy dedicated exp) together add 625 K gates, but ENABLE :")
    print("            • full activation function set (ReLU/GELU/Sigmoid/Tanh/")
    print("              SiLU/SwiGLU + sqrt/recip/log)")
    print("            • softmax exp at FULL cube-matched throughput (long-context")
    print("              attention now only 7 % slower than ideal)")
    print("            • all logical / comparison / masking / bit ops")
    print("           Net VEC area still drops 27 % vs naïve original, AND now")
    print("           keeps up with cube GEMM on long-context attention workloads.")
    print()
    print("  ─── Reference points  (28 nm, for context) ────────────────────────────")
    print()
    print("    block                                        gates")
    print("    ──────────────────────────────────────────   ──────")
    print("    4 KB SRAM macro  (1-port dense)              ~30 K  *")
    print("    4 KB DFF bank    (8K FF, full-port)          ~80 K")
    print("    Single FMA lane  (8 B, FP8/16/32/64)         ~10 K")
    print("    128 × FMA lane array                        ~1.28 M")
    print("    Single CMP_SWAP cell  (8 B + sel + dir)         ~50")
    print("    32-entry ROB w/ rename                         ~5 K")
    print("    Branch-tag spec tracker (8 lvl)                ~3 K")
    print("    L1 D-cache  (32 KB, 4-way set assoc)         ~500 K")
    print("    Davinci scalar core (in-order, 1 lane)       ~3-5 M")
    print("    Davinci OOO core (with VEC + acc paths)     ~10-15 M")
    print()
    print("    * RFS_A/B/RFA_A/B use DFF (not SRAM) because they need")
    print("      simultaneous multi-port access (read+RMW+write per cycle).")
    print("      A SRAM macro could shave ~50 K each but loses the multi-port,")
    print("      requiring banking → more area for banking logic. DFF is the")
    print("      right choice for this access pattern.")
    print()
    print("  ─── Where the 2.63 M gates go (visual percentage) ─────────────────────")
    print()
    print("    Per-lane FMA array      ███████████████████████████     48.7 %")
    print("    Storage RFs             ███████                         12.2 %")
    print("    64-copy dedicated exp   ██████                          12.2 %  ★ NEW")
    print("    16-copy general SF      ████                             8.2 %")
    print("    REDUCE tree             ███▍                             6.6 %")
    print("    Per-lane elem-wise ALU  █▊                               3.4 %")
    print("    SORT network            █▋                               3.2 %")
    print("    BROADCAST tree          █▎                               2.3 %")
    print("    Control + glue          ▊                                1.4 %")
    print("    K-wide RMW              ▌                                1.1 %")
    print("    Microcode ROM           ▏                                0.3 %")
    print("    ACC_SPILL latch         ▏                                0.1 %")
    print()
    print("  ─── Key takeaways ─────────────────────────────────────────────────────")
    print()
    print("    1) Per-lane FMA array still dominates  (49 %).  Untouched by these")
    print("       revisions ; future area work should target there.")
    print()
    print("    2) Dedicated exp + general SF together = 20 % of VEC, but enable :")
    print("       • Long-context softmax (N=32 K) at 7 % overhead vs cube GEMM")
    print("         (was 116 % overhead with K=8 SF only — 8× faster!)")
    print("       • SwiGLU/GELU at 1.9 % overhead (was 3.8 %)")
    print("       • All transcendental functions (sigmoid/tanh/log/sqrt/recip)")
    print("       Splitting exp into a dedicated unit (FP16 4-th minimax, 5 K g/")
    print("       copy) is the area/throughput sweet spot — costs 50 % less than")
    print("       scaling the general SF to the same exp throughput.")
    print()
    print("    3) Storage is now ~12 % of VEC (relatively smaller because compute")
    print("       grew). The 4 RFs × 4 KB are the fundamental cost of the 4 KB")
    print("       tile architecture ; cannot shrink without changing tile size.")
    print()
    print("    4) Sort + Reduce + Broadcast tree = ~12 % of VEC after revisions")
    print("       (was ~50 % before — the sort network alone was 39 % !).")
    print()
    print("    5) The K=16 + K=64 SF/exp choice :")
    print("       • K=16 general SF gives 1.9 % SwiGLU overhead, headroom for")
    print("         future activation functions and multi-context concurrency")
    print("       • K=64 dedicated exp gives 2× balanced for d_h=128 attention,")
    print("         enables software pipelining of softmax with next ·V GEMM,")
    print("         and is future-proof for next-gen larger cubes (8 K MAC/cy).")
    print("       • Combined +400 K g vs base, but enables long-context inference")
    print("         to be cube-bound (not exp-bound).")
    print()
    print("    6) Control logic stays trivial  (~2 %)  — instruction-class-driven")
    print("       design keeps the control path lightweight.")
    print()
    print("    7) The 0.97 M-gate net saving (vs naïve baseline) is enough to add :")
    print("        • another ROB level (for deeper OOO)            ~10 K")
    print("        • a 32 KB L1 D-cache                            ~500 K")
    print("        • a 16 KB L0 instruction cache                  ~80 K")
    print("        • or move toward a second VEC unit (~2 M)")
    print("       i.e. still enough budget for another major architectural feature.")
    print()
    print("    8) Five binary-class compute paths (FMA, ALU, general SF, dedicated")
    print("       exp, MOV) sit in PARALLEL, sharing the same operand read ports")
    print("       of RFS_A and RFS_B. A binary instruction is routed to EXACTLY")
    print("       ONE of them per cycle — they share lane bandwidth, not duplicate")
    print("       it. The instruction-class decode (~5 K g) handles routing.")
    print()
    print(hbar())
    print("  SF-COPY SCALING TRADE-OFF  (why K=16 general + K=64 dedicated exp)")
    print(hbar())
    print()
    print("  HISTORY : the original design proposed K=8 general SF (with exp")
    print("  inline). After analyzing softmax workload at long context, exp was")
    print("  separated into a dedicated unit, and the configuration was upgraded")
    print("  to K=16 general SF + K=64 dedicated exp. See --softmax and --exp-unit")
    print("  for the full justification.")
    print()
    print("  COMBINED SF/EXP SCALING (with exp separated) :")
    print()
    print("    config                       general SF   ded exp   total   FFN time")
    print("                                 (no exp)     (4-th)    cost    (long ctx)")
    print("    ─────────────────────────    ──────────   ───────   ──────  ──────────")
    print("    K=4 general only (extreme    90 K          —        90 K    1.55× ideal")
    print("       budget, edge inference)")
    print("    K=8 general only             135 K         —        135 K   1.58× ideal")
    print("    K=8 + K=32 dedicated exp     135 K         160 K    295 K   1.18× ideal")
    print("       (compact long-ctx)")
    print("    K=16 + K=64 dedicated exp ★  215 K         320 K    535 K   1.09× ideal")
    print("       (recommended default ★)")
    print("    K=16 + K=128 (1/lane) exp    215 K         640 K    855 K   1.04× ideal")
    print("       (extreme attention ASIC)")
    print()
    print("  ★ K=16 + K=64 is the recommended sweet spot :")
    print("    • Long-context attention only 7 % slower than ideal (cube-bound).")
    print("    • SwiGLU/GELU ≤ 2 % overhead on cube GEMM.")
    print("    • Headroom for new activation functions and multi-context serving.")
    print("    • Future-proof for next-gen 8 K-MAC cubes (K_balanced doubles).")
    print()
    print("  IMPLEMENTATION SKETCHES (K=16 general SF, K=64 dedicated exp):")
    print()
    print("    GENERAL SF (16 copies, 8 lanes/copy, 8 cy/tile):")
    print()
    print("       Lane 0..7    ──┐")
    print("       Lane 8..15   ──┤")
    print("       Lane 16..23  ──┤      ┌────────┐")
    print("       Lane 24..31  ──┼─────►│ 16-port│  banked SRAM holds")
    print("       ...           │      │ banked │  sigmoid/tanh/log/recip/")
    print("       Lane 120..127──┘      │  LUT/  │  rsqrt seed (NO exp tables)")
    print("                              │  seed  │  serves all 16 copies")
    print("                              └───┬────┘")
    print("                                  │")
    print("                ┌─────────────────┼─────────────────┐")
    print("           ┌────▼────┐       ┌────▼────┐       ┌────▼────┐")
    print("           │  SF #0  │       │  SF #1  │  ...  │  SF #15 │  each ~10 K g")
    print("           │ poly+NR │       │ poly+NR │       │ poly+NR │")
    print("           └────┬────┘       └────┬────┘       └────┬────┘")
    print("                │                 │                 │")
    print("           lane 0..7         lane 8..15         lane 120..127")
    print("           (8 cy MUX)        (8 cy MUX)         (8 cy MUX)")
    print()
    print("    DEDICATED EXP (64 copies, 2 lanes/copy, 4 cy/tile):")
    print()
    print("       Lane 0..1    ──┐")
    print("       Lane 2..3    ──┤      ┌─────────┐")
    print("       ...           │      │ shared  │  just 2 constants:")
    print("       Lane 124..125──┤      │  ln2    │  ln2 ≈ 0.6931, 1/ln2 ≈ 1.4427")
    print("       Lane 126..127──┘      │ 1/ln2   │")
    print("                              └────┬────┘")
    print("                                   │")
    print("              ┌────────────────────┼────────────────────┐")
    print("         ┌────▼────┐         ┌────▼────┐         ┌────▼────┐")
    print("         │ EXP #0  │         │ EXP #1  │   ...   │ EXP #63 │  each ~5 K g")
    print("         │range+   │         │range+   │         │range+   │  (FP16 4-th")
    print("         │ poly    │         │ poly    │         │ poly    │   minimax)")
    print("         └────┬────┘         └────┬────┘         └────┬────┘")
    print("              │                   │                   │")
    print("         lane 0..1            lane 2..3           lane 126..127")
    print("         (4 cy MUX)           (4 cy MUX)          (4 cy MUX)")
    print()
    print("  THROUGHPUT COMPARISON :")
    print("    Per-lane FMA  : 128 elem/cy → 1 tilelet in  1 cy  (baseline)")
    print("    General SF    :  16 elem/cy → 1 tilelet in  8 cy  (8× slower)")
    print("    Dedicated exp :  64 elem/cy → 1 tilelet in  4 cy  (4× slower)")
    print("    With cube C=4096 MACs/cy, dedicated exp matches cube exactly.")
    print()


# ---------------------------------------------------------------------------
# Softmax performance analysis  (does exp / div need extra acceleration ?)
# ---------------------------------------------------------------------------

def print_softmax_analysis() -> None:
    print(hbar())
    print("  SOFTMAX  PERFORMANCE  ANALYSIS")
    print("  (Do we need to specifically accelerate exp / div to match cube GEMM ?)")
    print(hbar())
    print()
    print("  ─── Softmax operation breakdown ───────────────────────────────────────")
    print()
    print("    softmax(x_i) = exp(x_i - max(x))  /  Σⱼ exp(xⱼ - max(x))")
    print()
    print("    For a row of N elements :")
    print()
    print("      step  op            hardware             throughput (per cycle)")
    print("      ────  ──────────    ─────────────────    ──────────────────────")
    print("      1     max          REDUCE tree           1 element / cy (pipelined)")
    print("      2     sub max      per-lane ALU          128 elements / cy")
    print("      3     exp          SF engine  ★          K elements / cy   ← bottleneck")
    print("      4     sum          REDUCE tree           1 element / cy")
    print("      5     1 / sum      SF engine (recip)     1 op / row  (scalar)")
    print("      6     mul (1/sum)  per-lane FMA          128 elements / cy")
    print()
    print("  ─── KEY INSIGHT : div is essentially FREE ─────────────────────────────")
    print()
    print("    Naïve view     :  N divisions per row   →  N SF ops per row")
    print("    Smart impl     :  1 reciprocal + N multiplications")
    print("                      = 1 SF op + N per-lane FMA ops")
    print()
    print("    Cycles per row for div phase :")
    print("      • 1 / sum  :  ~6-8 cy   (one SF op, scalar)")
    print("      • mul      :  N / 128  cy   (per-lane FMA)")
    print("      • for N=512 row :  8 + 4 = 12 cy total")
    print()
    print("    → div is NOT the bottleneck. No special hardware needed for div.")
    print("    → only EXP throughput matters.")
    print()
    print("  ─── Cube vs SF throughput balance ─────────────────────────────────────")
    print()
    print("    Let  C  = cube MAC throughput          (e.g. 16×16×16 = 4096 MACs/cy)")
    print("         K  = SF copies in VEC unit        (1, 8, 16, 32, 128)")
    print("         d_h = attention head dim          (typically 64-256)")
    print("         N   = sequence length             (typically 128-32 K)")
    print()
    print("    Attention forward pass per head :")
    print()
    print("      Q·Kᵀ  : N²·d_h MACs    →  N²·d_h / C   cycles  (cube)")
    print("      softmax exp : N² ops   →  N² / K       cycles  (SF)")
    print("      ·V    : N²·d_h MACs    →  N²·d_h / C   cycles  (cube)")
    print()
    print("    BALANCE  :   N²·d_h / C  =  N² / K")
    print("                        K_balanced  =  C / d_h")
    print()
    print("    For typical Davinci cube  C = 4096 MACs/cy  (FP16) :")
    print()
    print("      d_h (head dim)    K_balanced     comment")
    print("      ──────────────    ──────────     ─────────────────────────────────")
    print("      256               16             K=8 sufficient (2× slower OK)")
    print("      128 (typical)     32             K=8 = 4× slow ; K=16 = 2× ; K=32 ★")
    print("      64                64             K=8 = 8× slow ; K=32 = 2× ; K=64 ★")
    print("      32  (small head)  128            need 1 SF / lane to match")
    print()
    print("  ─── Full Transformer inference  (LLaMA-7B class) ─────────────────────")
    print()
    print("    Per Transformer block per token (d=4096, H=32, d_h=128) :")
    print()
    print("      • QKV proj + out proj + 2× FFN  =  12·N·d²        GEMM MACs")
    print("      • Q·Kᵀ + ·V                     =  2·N²·d         GEMM MACs")
    print("      • Softmax exp                   =  H·N²           SF ops")
    print("      • GELU                          =  4·N·d          SF ops")
    print()
    print("    With cube C = 4096 MACs/cy and various K (SF copies) :")
    print()
    print("    seq N    GEMM cy    Exp cy K=1     K=8        K=16       K=32")
    print("    ─────    ───────    ──────────     ────────   ────────   ────────")
    print("    128      6.3 M      2.6 M  (40%)   325 K (5%) 162 K (3%) 81 K (1%)")
    print("    2 K      108 M      167 M (155%)   21 M  (19%) 10 M (10%) 5 M  (5%)")
    print("    8 K      540 M      2.1 G (390%)   268 M (50%) 134 M(25%) 67 M (12%)")
    print("    32 K     3.7 G      34 G  (920%)   4.3 G(116%) 2.1 G(57%) 1.07 G(29%)")
    print()
    print("    (% = exp time as fraction of GEMM time. > 100 % means exp is the")
    print("     dominant bottleneck even after the cube finishes its work.)")
    print()
    print("  ─── Recommendations by workload ───────────────────────────────────────")
    print()
    print("    NOTE : the FINAL design splits exp into a DEDICATED unit. The")
    print("    'K' below counts dedicated-exp copies (after exp is removed from")
    print("    general SF). See --exp-unit for the full design split.")
    print()
    print("    target workload                       config            VEC area")
    print("    ───────────────────────────────────   ───────────────   ──────────")
    print("    Edge / mobile (N ≤ 1 K)               K_gen=4 + K_exp=32 2.27 M")
    print("    Compact long-ctx (N ≤ 8 K)            K_gen=8 + K_exp=32 2.39 M")
    print("    Long-context infer (N = 16 K-32 K) ★  K_gen=16 + K_exp=64 2.63 M ★")
    print("       (NEW DEFAULT)")
    print("    Extreme long-ctx (N > 32 K)           K_gen=16 + K_exp=128 2.95 M")
    print("    Training (FP32 backprop)              K_gen=16 (exp inline) 2.30 M")
    print("                                          (no dedicated exp ; need")
    print("                                           FP32 accuracy in N-R)")
    print()
    print("  ─── Alternative optimizations  (don't just throw K at it) ────────────")
    print()
    print("    1) ONLINE SOFTMAX (FlashAttention-style)")
    print("       Fuse max + exp + sum + div into ONE pass per row.")
    print("       Same exp count, but :")
    print("        • saves N²·E bytes of intermediate storage / DMA")
    print("        • allows attention tile fusion with cube GEMM")
    print("        • does NOT reduce exp bottleneck — orthogonal to K.")
    print()
    print("    2) LOW-PRECISION EXP  (FP16 piecewise + 16-entry LUT)")
    print("       Use 16-segment polynomial + small LUT, accuracy ≈ 2 ULP.")
    print("       Per SF copy: ~3 K gates instead of ~10 K.")
    print("       Allows K = 16-32 within the same area as K = 8 today.")
    print("       Suitable for inference (training may need higher precision).")
    print()
    print("    3) i-BERT / I-EXP STYLE  (8-segment polynomial, no LUT)")
    print("       Pure polynomial, no LUT — datapath ~3 K g per copy.")
    print("       Easy to replicate to K = 16-32 cheaply.")
    print("       Drop-in for INT8/FP8 quantized softmax (now standard in LLM infer).")
    print()
    print("    4) DEDICATED ATTENTION-EXP UNIT  (orthogonal to general SF)")
    print("       Build 16-32 narrow exp-only units (no sigmoid/log/sqrt) :")
    print("        • single exp-only datapath  : ~5 K gates")
    print("        • 16 copies                : 80 K gates")
    print("        • 32 copies                : 160 K gates")
    print("       Cheaper than scaling general SF engine to K=16-32 (would cost")
    print("       ~75-225 K gates due to N-R, sigmoid, log overhead per copy).")
    print()
    print("       Cost comparison for K=16-equivalent attention exp throughput :")
    print()
    print("         option                                   gates   notes")
    print("         ────────────────────────────────────     ─────   ─────────")
    print("         scale general SF to K=16                 +75 K   reuses logic")
    print("         dedicated 16-copy attention-exp unit     +80 K   parallel path")
    print("         scale general SF to K=32                +225 K   serves all SF")
    print("         dedicated 32-copy attention-exp unit    +160 K   parallel path")
    print()
    print("       VERDICT : a dedicated exp-only path is competitive, especially")
    print("       for long-context attention-heavy designs.")
    print()
    print("  ─── Should we boost div ?  NO ────────────────────────────────────────")
    print()
    print("    div in softmax = 1 reciprocal + N multiplies, NOT N divisions.")
    print("    The reciprocal is ONE scalar SF op per row — a single SF copy at")
    print("    K=1 already handles it in 8 cy. The multiplies are per-lane FMA at")
    print("    full 128 elem/cy throughput.")
    print()
    print("    Even for layer-norm (which has /σ), we use 1 rsqrt + N muls.")
    print("    DEDICATED div hardware is NEVER worth it for DL workloads.")
    print()
    print("  ─── Final verdict  (quick reference) ──────────────────────────────────")
    print()
    print("    Question                                Answer")
    print("    ─────────────────────────────────────   ──────────────────────────")
    print("    Need to boost div ?                     NO  (recip + mul is enough)")
    print("    Need to boost exp for short context ?   NO  (K=8 already enough)")
    print("    Need to boost exp for typical infer ?   K=8 OK, K=16 better")
    print("    Need to boost exp for long context ?    YES → K=16 or K=32")
    print("    Best low-cost boost ?                   FP16 piecewise exp in SF")
    print("    Best high-perf option ?                 Dedicated 16-32 copy exp")
    print("    Use FlashAttention ?                    YES — orthogonal benefit")
    print()


# ---------------------------------------------------------------------------
# Dedicated exp unit  (separate exp from SF, scale to K=32)
# ---------------------------------------------------------------------------

def print_dedicated_exp_design() -> None:
    print(hbar())
    print("  DEDICATED  EXP  UNIT  DESIGN")
    print("  (Q1: i-BERT exp accuracy ?  Q2: is K=32 dedicated exp enough ?)")
    print(hbar())
    print()
    print("  ─── Q1 : i-BERT exp accuracy analysis ──────────────────────────────")
    print()
    print("  i-BERT (Kim et al, ICML'21) approximates exp via range reduction +")
    print("  2nd-order polynomial in a small range :")
    print()
    print("    Step 1  range reduction :  exp(x) = exp(p) · 2^k")
    print("                               where  k = ⌊-x / ln2⌋")
    print("                                      p = x + k·ln2  ∈ [-ln2, 0]")
    print()
    print("    Step 2  polynomial in p ∈ [-ln2, 0] :")
    print("              i-BERT (2nd) :  exp(p) ≈ 0.3585·(p + 1.353)² + 0.344")
    print("              4th order    :  exp(p) ≈ Σ aᵢ·pⁱ  (5 coefficients)")
    print("              5th order    :  6 coefficients")
    print()
    print("    Step 3  apply scale  :  result = poly(p) << k   (integer shift)")
    print()
    print("  Range reduction is essentially FREE (~50 g for the integer extract +")
    print("  multiply-by-ln2). The polynomial dominates cost AND accuracy.")
    print()
    print("  ACCURACY vs COST per exp unit copy :")
    print()
    print("    impl                      max rel err     equivalent prec   gates/copy")
    print("    ──────────────────────    ───────────     ──────────────    ─────────")
    print("    i-BERT  (2nd order)       ~1 %            INT8 quant only      2.5 K")
    print("    4th-order minimax         ~0.1 %          FP16 (~2 ULP)        5 K  ★")
    print("    5th-order minimax         ~0.01 %         FP16 (~1 ULP)        6 K")
    print("    2nd order + 16-entry LUT  ~0.05 %         FP16 (~1 ULP)        3 K + LUT")
    print("    Full SF exp (6th + LUT)   < 1 ULP         FP32                10 K")
    print()
    print("  → The classic i-BERT (2nd order) loses precision badly (~1 % rel err)")
    print("    — fine for INT8 quantized inference, but TOO COARSE for FP16.")
    print()
    print("  → 4th-order minimax matches FP16 precision (~2 ULP) at ~5 K gates/copy.")
    print("    This is the sweet spot for FP16 LLM inference. ★")
    print()
    print("  → For mixed-precision training or HPC, must keep exp inside the full")
    print("    SF unit (10 K g) — cannot use the cheap dedicated path.")
    print()
    print("  ─── Q2 : Is K=32 dedicated exp enough ? ───────────────────────────")
    print()
    print("  ATTENTION SERIAL PATH (per head, per token, dominant at long ctx) :")
    print()
    print("      Q · Kᵀ           : N² · d_h / C       cycles  (cube)")
    print("      softmax exp      : N² / K_exp         cycles  (dedicated)")
    print("      mul by (1/sum)   : N² / 128           cycles  (per-lane FMA)")
    print("      · V              : N² · d_h / C       cycles  (cube)")
    print()
    print("  BALANCE  :  N² / K_exp  ≤  N² · d_h / C")
    print()
    print("              K_exp_min  =  C / d_h")
    print()
    print("  For typical Davinci cube  C = 4096 MACs/cy (FP16) :")
    print()
    print("    d_h    K_exp_min   model examples           K=32 enough ?")
    print("    ───    ─────────   ──────────────────────   ─────────────")
    print("    256    16          ViT-Huge, large LLM      YES (overkill)")
    print("    128    32          LLaMA, Mistral, GPT-3 ★  YES (exact match)")
    print("     96    43          some smaller LLMs        ALMOST (1.3× exp)")
    print("     64    64          old transformers         NO (need K=64)")
    print("     32   128          rare                     NO (need K=128)")
    print()
    print("  → For ALL modern LLMs (d_h ≥ 128), K=32 exactly balances exp with cube.")
    print()
    print("  ─── Long-context attention measurement (LLaMA-7B, N=32K) ──────────")
    print()
    print("  config                            exp count  exp cy   exp/GEMM   atten")
    print("                                                                   total")
    print("                                                                   (vs ideal)")
    print("  ──────────────────────────────    ─────────  ──────   ────────   ─────")
    print("  K=8 general SF only (current)          8     4.3 G    116 %     1.58×")
    print("  K=16 general SF                       16     2.1 G     57 %     1.29×")
    print("  K=32 general SF                       32     1.07 G    29 %     1.14×")
    print("  K=8 general + K=32 dedicated exp ★    32     1.07 G    29 %     1.14× ★")
    print("  K=8 general + K=64 dedicated exp      64     0.54 G    14 %     1.07×")
    print("  K=8 general + K=128 (1/lane) exp     128     0.27 G     7 %     1.04×")
    print()
    print("  ★ K=32 dedicated exp gives the SAME attention performance as K=32")
    print("    general SF (1.14× ideal), because attention exp throughput is the")
    print("    only thing that matters here.")
    print()
    print("  → K=32 DEDICATED EXP IS ENOUGH for long-context LLM inference :")
    print("    • Attention overhead drops from 58 % slowdown to 14 %.")
    print("    • Going to K=64 only saves another 7 % — diminishing returns.")
    print("    • K=128 (1/lane) saves only 3 % more — never worth it.")
    print()
    print("  ─── Cost comparison : dedicated exp vs scaling general SF ─────────")
    print()
    print("  Scaling general SF replicates sqrt/log/sigmoid (which attention does")
    print("  NOT use). Dedicated exp replicates ONLY the exp path :")
    print()
    print("    config                          general SF   ded.exp   TOTAL  delta")
    print("                                    cost         cost      cost")
    print("    ────────────────────────────    ──────────   ───────   ─────  ──────")
    print("    K=8 general only (baseline)      135 K        —        135 K     0")
    print("    K=16 general (no ded. exp)       210 K        —        210 K   +75 K")
    print("    K=32 general (no ded. exp)       360 K        —        360 K  +225 K")
    print("    K=8 + K=32 i-BERT exp (INT8)     135 K       80 K      215 K   +80 K")
    print("    K=8 + K=32 4th-order exp (FP16★) 135 K      160 K      295 K  +160 K  ★")
    print("    K=8 + K=64 4th-order exp (FP16)  135 K      320 K      455 K  +320 K")
    print("    K=4 + K=32 4th-order exp          90 K      160 K      250 K  +115 K")
    print()
    print("  → K=8 general + K=32 dedicated 4th-order exp = +160 K vs +225 K for")
    print("    K=32 general. Saves 65 K AND keeps all 6 SF functions at K=8 throughput.")
    print()
    print("  → For INT8/FP8 quantized inference (modern LLM recipe), use i-BERT")
    print("    2nd-order exp : K=32 only +80 K, total 215 K. Cheapest viable option.")
    print()
    print("  ─── Why dedicated exp is much cheaper than general SF ─────────────")
    print()
    print("  General SF (10 K g/copy) breakdown :")
    print()
    print("      ┌────────────────────────────────────────┐")
    print("      │ exp/log piecewise + range reduction   │  4 K g  ← we want THIS")
    print("      │ sigmoid/tanh LUT                      │  1.5 K  (could share)")
    print("      │ Newton-Raphson (sqrt / recip)         │  3 K    ← attention doesn't use")
    print("      │ polynomial datapath + MUX             │  2 K")
    print("      │ sequencer + control                   │  1.5 K")
    print("      └────────────────────────────────────────┘")
    print()
    print("    → Replicating general SF wastes 50 % on sqrt/log/sigmoid that")
    print("      attention doesn't need.")
    print()
    print("  Dedicated exp (5 K g/copy, 4th-order minimax FP16) :")
    print()
    print("      ┌────────────────────────────────────────┐")
    print("      │ range reduction (k, p split)          │  0.5 K")
    print("      │ 4-coeff poly multiply-accumulate      │  3.5 K")
    print("      │ scale (k-bit shift) + format conv     │  0.5 K")
    print("      │ small sequencer                       │  0.5 K")
    print("      └────────────────────────────────────────┘")
    print()
    print("    → Half the cost, exp-only. 32 copies = 160 K g (vs 320 K g if we")
    print("      scaled general SF to K=32 just for exp throughput).")
    print()
    print("  ─── Implementation sketch : 32-copy dedicated exp unit ────────────")
    print()
    print("    Lane 0..3   ──┐")
    print("    Lane 4..7   ──┤    ┌─────────┐")
    print("    Lane 8..11  ──┼───►│ shared  │   range reduction tables")
    print("    Lane 12..15 ──┤    │ ln2     │   (just two constants : ln2, 1/ln2)")
    print("    ...           │    │ const   │")
    print("    Lane 124..127─┘    └────┬────┘")
    print("                            │")
    print("    ┌─────────────┬─────────┼─────────┬─────────────┐")
    print("    │             │         │         │             │")
    print("  ┌─▼──┐        ┌─▼──┐    ┌─▼──┐    ┌─▼──┐        ┌─▼──┐")
    print("  │EXP │        │EXP │    │EXP │    │EXP │ ...    │EXP │  32 copies")
    print("  │ #0 │        │ #1 │    │ #2 │    │ #3 │        │ #31│  ~5 K g each")
    print("  │range+      │range+│   │range+│   │range+│      │range+│  (FP16 4-th")
    print("  │ poly │     │ poly │   │ poly │   │ poly │      │ poly │   minimax)")
    print("  └─┬──┘        └─┬──┘    └─┬──┘    └─┬──┘        └─┬──┘")
    print("    │             │         │         │             │")
    print("   lane 0..3    4..7      8..11    12..15        124..127")
    print("   (4 cy MUX)   (4 cy)    (4 cy)   (4 cy)         (4 cy)")
    print()
    print("  Per-copy throughput : 1 elem/cy  →  4 lanes/copy time-muxed in 4 cy.")
    print("  Total throughput    : 32 elem/cy → entire 128-lane tile in 4 cy.")
    print("  Pipeline depth      : 5 cy (range red 1 + poly 3 + scale 1).")
    print()
    print("  COMPARED TO PER-LANE FMA  :")
    print("    FMA throughput  : 128 elem/cy → 1 tilelet in 1 cy")
    print("    Exp throughput  : 32  elem/cy → 1 tilelet in 4 cy")
    print("    Exp is now only 4× slower than FMA per tilelet.")
    print("    Was 128× slower with K=1 SF, 16× with K=8.")
    print()
    print("  ─── Why K=8 general was originally chosen, and why we're upsizing ────")
    print()
    print("  ORIGIN of K=8 general SF :")
    print("    K=8 was sized BEFORE exp was offloaded to a dedicated unit. At")
    print("    that time, exp was the dominant SF load (softmax + softplus +")
    print("    GELU-via-erf/exp etc.), so K=8 was the sweet spot for exp")
    print("    throughput. Now that exp is offloaded :")
    print()
    print("    Workload        ops/layer (LLaMA, N=2K)    K=8 cycles    % of GEMM")
    print("    ──────────      ──────────────────────     ──────────    ─────────")
    print("    SwiGLU sigmoid  4·N·d = 33 M               4.1 M cy      3.8 %")
    print("    LayerNorm rsq   2 per layer (negligible)   ~ 0           ~0 %")
    print("    log / div       inference rarely uses      ~ 0           ~0 %")
    print()
    print("    → After exp offload, K=8 has 50 % unused capacity. K=4 would")
    print("      already give 7.6 % overhead — also acceptable. So why K=16 ?")
    print()
    print("  REASONS TO UPSIZE GENERAL SF TO K=16 :")
    print("    • Future activation functions (Mish, Swish-2, GeLU exact) may add")
    print("      tanh / sigmoid ops not currently counted.")
    print("    • Software pipelining : overlap activation with next FFN GEMM.")
    print("    • Multi-context concurrent execution (KV-cache batched serving).")
    print("    • LayerNorm + SwiGLU together can spike the SF queue briefly.")
    print("    • Cost is moderate : 8→16 = +80 K gates (LUT shared, only datapath")
    print("      replicates).")
    print()
    print("  ─── Format/precision support of each SF flavor ────────────────────")
    print()
    print("    block                 supported formats        gates / copy")
    print("    ──────────────────    ──────────────────       ────────────")
    print("    General SF (default)  FP16 + FP32 mixed        ~10 K  (LUT shared)")
    print("                          (sigmoid, tanh, exp,     (~7 K datapath +")
    print("                           log, sqrt, recip)       ~3 K LUT-share share)")
    print("    Dedicated exp         FP16 only                ~5 K (4-th minimax)")
    print("                                                   ~6 K (FP16 + FP32)")
    print("                                                   ~2.5 K (i-BERT 2nd-")
    print("                                                          order, INT8)")
    print()
    print("    → General SF is NOT FP32-only ; it's mixed FP16+FP32. Could be")
    print("      restricted to FP16-only (~6 K g/copy) for inference-only ASIC,")
    print("      saving ~4 K g per copy.")
    print()
    print("    → Dedicated exp is FP16-only by default (matches inference workloads).")
    print("      Can be upgraded to FP16+FP32 (+1 K g/copy) if training is in scope.")
    print()
    print("  ─── UPDATED FINAL recommendation matrix ─────────────────────────────")
    print()
    print("    target product           recommended config           VEC area")
    print("    ──────────────────────   ──────────────────────       ────────")
    print("    Edge inference (FP8)     K=4 general + K=32           2.27 M")
    print("                             i-BERT 2nd-order exp          (+45 K)")
    print("    Mainstream LLM infer     K=8 general + K=32           2.39 M")
    print("    (FP16, N ≤ 8K)           4th-order minimax exp         (+160 K)")
    print("    Long-context infer       K=16 general + K=64          2.63 M ★ NEW")
    print("    (FP16, N = 16K - 128K)   4th-order minimax exp         (+400 K)")
    print("       ← user's preferred default ★")
    print("    Pure attention ASIC      K=16 general + K=128         2.95 M")
    print("    (extreme long ctx)       4th-order minimax exp         (+720 K)")
    print("    Training (mixed prec)    K=16 general (exp inline)    2.30 M")
    print("                             FP32 capable                   (+75 K)")
    print()
    print("  ─── DEFAULT CHANGE : K=16 general + K=64 dedicated exp ★ ─────────")
    print()
    print("    Per user's recommendation, the FINAL DEFAULT for the long-context")
    print("    LLM inference target is :")
    print()
    print("      • General SF       : K=16 mixed FP16+FP32     →  215 K g")
    print("      • Dedicated exp    : K=64 FP16 4-th minimax   →  320 K g")
    print("      • Combined SF/exp  :                          →  535 K g")
    print()
    print("    vs old K=8 + K=32 design  : 295 K g  →  delta +240 K g (+1.4 % VEC)")
    print("    vs original K=8 only base : 135 K g  →  delta +400 K g")
    print()
    print("    PERFORMANCE (LLaMA-7B inference, N = 32 K) :")
    print()
    print("      metric                         K=8 + K=32      K=16 + K=64 ★")
    print("      ──────────────────────────     ───────────     ──────────────")
    print("      attention time vs ideal        1.14×           1.07×")
    print("      SwiGLU time / GEMM time        3.8 %           1.9 %")
    print("      total inference vs ideal       1.18×           1.09×")
    print()
    print("    +240 K gates (+10 % VEC area) gives ~9 % faster total inference")
    print("    on long-context workloads.  Excellent trade for inference-")
    print("    optimized ASICs ; for edge/short-context, the cheaper K=8 + K=32")
    print("    config is recommended.")
    print()
    print("  ─── Why K=64 exp matters even though K=32 'balances' attention ───")
    print()
    print("  K_balanced = C / d_h gives the MINIMUM K to not bottleneck.  K=64")
    print("  gives 2× headroom which enables :")
    print()
    print("    1) SOFTWARE PIPELINING : while ·V runs on cube, next head's exp")
    print("       can start in parallel — exp is no longer on critical path.")
    print()
    print("    2) SMALLER d_h MODELS : ViT-Small (d_h=64), some MoE expert heads")
    print("       (d_h=64-96), etc. K=32 doesn't balance these (need K=64-96).")
    print()
    print("    3) MULTI-CONTEXT : KV-cache batched serving runs multiple")
    print("       attentions concurrently. K=64 gives margin for 2× concurrency.")
    print()
    print("    4) BIGGER CUBE FUTURE : if cube grows to 8 K MACs/cy (next gen),")
    print("       K_balanced doubles to 64. K=64 today = future-proof.")
    print()
    print("  All four reasons argue for the 2× headroom. K=64 is correct for a")
    print("  forward-looking long-context inference design.")
    print()
    print("  ─── Datapath integration ────────────────────────────────────────────")
    print()
    print("  Both general SF (K=16) and dedicated exp (K=64) sit in PARALLEL")
    print("  with the per-lane FMA and element-wise ALU. Instruction-class")
    print("  decode routes :")
    print()
    print("    op type                        →  routed to")
    print("    ────────────────────────────      ─────────────────────")
    print("    FMUL/FADD/FMA/FNMA/VLERP          per-lane FMA array (128 lanes)")
    print("    AND/OR/XOR/CMP/MIN/MAX/SHIFT      per-lane elem-wise ALU (128 lanes)")
    print("    SIGMOID/TANH/LOG/SQRT/RECIP       general SF engine (16 copies)")
    print("    EXP / SOFTMAX-fused-EXP           dedicated exp unit (64 copies)")
    print("    REDUCE_*  (sum/max/min/argmax)    REDUCE tree (1 path)")
    print("    SORT / TOPK_LARGE                 time-muxed sort network")
    print("    BROADCAST_* / EXPAND              broadcast tree")
    print("    MOV / COPY / MASK_APPLY           DMA-style mover")
    print()
    print("  Each cycle : ONE instruction issued, routed to ONE compute path.")
    print("  All paths share the same operand read ports of RFS_A / RFS_B /")
    print("  RFA_A / RFA_B (4 KB each). No additional storage cost.")
    print()


# ---------------------------------------------------------------------------
# REVISION : time-multiplexed sort network (trade speed for area)
# ---------------------------------------------------------------------------

def print_time_muxed_sort_revision() -> None:
    print(hbar())
    print("  REVISED SORT NETWORK : TIME-MULTIPLEXED  (trade speed for area)")
    print(hbar())
    print()
    print("  This section SUPERSEDES the area estimate in --sort-network. The")
    print("  earlier section quoted ~28 K cells (~1.4 M gates) for a fully-")
    print("  pipelined N=1024 bitonic sort. Here we exploit the fact that every")
    print("  stage of bitonic sort uses the SAME cell type (CMP_SWAP), differing")
    print("  only in WIRING and DIRECTION pattern. Build ONE stage and time-mux")
    print("  it across 55 beats — same latency, ~16× area saving.")
    print()
    print("  ─── Insight ───────────────────────────────────────────────────────────")
    print()
    print("    Full pipeline  =  SPACE expansion  : 55 stages physically present,")
    print("                                          1 sort flows through in 55 cy")
    print("                                          (latency)  AT 1 sort/cy")
    print("                                          (throughput, after fill)")
    print()
    print("    Time-muxed     =  TIME expansion   : 1 stage physically, looped")
    print("                                          55 times for 1 sort = 55 cy")
    print("                                          latency, 1/55 sort/cy")
    print()
    print("    LATENCY for a single sort is the SAME (55 cycles either way).")
    print("    Only THROUGHPUT differs (55× lower for time-muxed).")
    print()
    print("    Sort is rare in DL inference (top-K sampling, beam search,")
    print("    occasional median) → throughput penalty is invisible. ⇒ time-mux.")
    print()
    print("  ─── Four-option comparison ────────────────────────────────────────────")
    print()
    print("    option                        cells      ~gates    latency   throughput")
    print("    ───────────────────────────   ────────   ───────   ───────   ──────────")
    print("    A. Full pipeline (current)    28160      ~1.4 M    55 cy     1 sort/cy")
    print("    B. 1-stage time-muxed         512        ~85 K     55 cy     1/55 cy")
    print("    C. K-stage part-pipelined     K·512      ~K·25 K   55 cy     K/55 cy")
    print("       (K=5)                      2560       ~250 K    55 cy     1/11 cy")
    print("       (K=11)                     5632       ~310 K    55 cy     1/5  cy")
    print("    D. Reuse REDUCE L0 cells      0  (free)  ~70 K *   55 cy *   1/55 cy *")
    print()
    print("    * D's incremental cost ; sort blocks REDUCE during its 55 cy")
    print()
    print("    All options have IDENTICAL latency.  B and D save ~16-20× area")
    print("    over A.  Recommended : B for clean modularity, D for tightest area.")
    print()
    print("  ─── Architecture B : 1-stage time-multiplexed sort ────────────────────")
    print()
    print("                     RFS_A   (4 KB)   ← sort scratch (no extra FF needed)")
    print("                        │")
    print("                   ┌────┴────┐")
    print("                   │         │")
    print("              read 1024 B   write 1024 B")
    print("                   │         │")
    print("                   ▼         ▲")
    print("            ┌──────────────────────────┐")
    print("            │  PERMUTE NETWORK          │")
    print("            │  configurable butterfly,  │")
    print("            │  stride d ∈ {1,2,4,...,   │")
    print("            │  256,512}                  │")
    print("            │  10 levels × 1024 MUX     │")
    print("            │  ≈ 50 K gates             │")
    print("            └─────────────┬─────────────┘")
    print("                          │ paired lanes")
    print("                          ▼")
    print("            ┌──────────────────────────┐")
    print("            │  1 STAGE OF SORT NETWORK  │")
    print("            │  512 × CMP_SWAP cells     │")
    print("            │  per-cell asc/desc bit    │")
    print("            │  ≈ 25 K gates             │")
    print("            └─────────────┬─────────────┘")
    print("                          │")
    print("                          ▼")
    print("                      back to RFS_A")
    print()
    print("            ┌──────────────────────────┐")
    print("            │  SEQUENCER + SCHEDULE ROM │")
    print("            │  for stage in 0..54 :     │")
    print("            │    (d, dir_pat) = ROM[s]  │")
    print("            │    drive permute & cells  │")
    print("            │  ≈ 10 K gates             │")
    print("            └──────────────────────────┘")
    print()
    print("  ─── Detailed cost breakdown for option B ──────────────────────────────")
    print()
    print("    block                                        ~gates")
    print("    ──────────────────────────────────────────   ───────")
    print("    Permute network (10-level butterfly,")
    print("       1024 byte-MUXes per level × 10 levels        50 K")
    print("       × 5 gates per byte-MUX)")
    print("    CMP_SWAP cells (512 × 50 gates)                25 K")
    print("    Per-cell direction logic (5 K) +")
    print("       sequencer ROM/FSM (5 K)                     10 K")
    print("    State storage (use RFS_A as scratch)            0")
    print("    ──────────────────────────────────────────   ───────")
    print("    TOTAL                                         ~85 K")
    print()
    print("    vs full pipeline (option A) : ~1.4 M gates  ⇒  16.5× reduction")
    print()
    print("  ─── Permute network design (configurable butterfly) ───────────────────")
    print()
    print("    Bitonic sort's per-stage wiring is always 'pair lane k with lane k+d'")
    print("    for some stride d ∈ {1, 2, 4, ..., 512}. This is the perfect-shuffle")
    print("    pattern, which is what a butterfly network does.")
    print()
    print("    A 10-LEVEL configurable butterfly works as :")
    print()
    print("           level 0 : optional swap at stride 1   (stride-1 perm)")
    print("           level 1 : optional swap at stride 2")
    print("           level 2 : optional swap at stride 4")
    print("           ...")
    print("           level 9 : optional swap at stride 512")
    print()
    print("    Per-level enable bit picks 'apply this stride' or 'pass through'.")
    print("    To realise stride-d for the current sort stage, enable level log₂(d)")
    print("    and disable all others.")
    print()
    print("    Cost : 10 levels × 1024 byte positions × 1 byte-MUX (8b 2:1 + 1 sel)")
    print("           = 10 K MUXes × ~5 gates each ≈ 50 K gates")
    print()
    print("  ─── Direction pattern logic ───────────────────────────────────────────")
    print()
    print("    Each cell needs an asc/desc bit per stage. For Batcher's bitonic, the")
    print("    direction pattern is determined by (phase_k, sub_block_size) and is")
    print("    a function of cell_index :")
    print()
    print("        dir(k, j, cell) = bit_j_of_(cell >> log₂(d))   (or similar)")
    print()
    print("    Either compute combinationally (~10 gates / cell × 512 = 5 K), or")
    print("    pre-compute and store in a 55 × 512-bit pattern ROM (~3 KB ROM).")
    print()
    print("  ─── Sequencer / SCHEDULE ROM ──────────────────────────────────────────")
    print()
    print("    State :  6-bit stage counter (0..54)")
    print("    ROM   :  for each of 55 stages, store :")
    print("               • stride d                    4 bits (log₂ {1..512})")
    print("               • direction pattern id         4 bits")
    print("               • TOP-truncate skip flag       1 bit (per format)")
    print("               • BOTTOM-truncate stop flag    1 bit (per shape)")
    print("             total ~12 bits × 55 stages = 660 bits ROM")
    print()
    print("    FSM controls the 55-cycle sequencing, applying TOP/BOTTOM truncation")
    print("    by SKIPPING stages in the count (saves more cycles for narrow")
    print("    formats / sub-tile sorts).")
    print()
    print("  ─── Why throughput drops to 1/55  (resource-conservation view) ────────")
    print()
    print("    Total work for one N=1024 bitonic sort :")
    print("       55 stages × 512 cells = 28160 CMP_SWAP operations")
    print()
    print("    Hardware throughput (ops per cycle) :")
    print("       option A (full pipeline) : 28160 cells, each 1 op/cy → 28160 op/cy")
    print("       option B (1-stage muxed) :   512 cells, each 1 op/cy →   512 op/cy")
    print()
    print("    Sort throughput =  cells_in_use_per_cycle / cells_needed_per_sort")
    print()
    print("       option A : 28160 / 28160 = 1 sort/cy")
    print("       option B :   512 / 28160 = 1/55 sort/cy")
    print()
    print("    The 1/55 is a HARD resource-conservation limit, not an artefact of")
    print("    a particular schedule. No clever pipelining can change it because")
    print("    the total ops needed is fixed and the ops/cy supplied is fixed.")
    print()
    print("  ─── Why pipelining cannot rescue option B ─────────────────────────────")
    print()
    print("    Normal pipeline (option A) runs at 1 sort/cy because there are 55")
    print("    INDEPENDENT physical stages, each doing 1 op-stage of work for a")
    print("    DIFFERENT in-flight sort :")
    print()
    print("        cycle 100 :  sort_A at stage 54   ← completes this cycle")
    print("                     sort_B at stage 53")
    print("                     sort_C at stage 52")
    print("                     ...")
    print("                     sort_X at stage 0    ← starts this cycle")
    print()
    print("        55 sorts in flight, 1 sort retires per cycle.")
    print()
    print("    Time-muxed (option B) has only 1 physical stage — there is no")
    print("    'spatial pipeline' to fill. Trying to interleave two sorts only")
    print("    halves each one's progress :")
    print()
    print("        cycle 0   :  stage_0 of sort_A")
    print("        cycle 1   :  stage_0 of sort_B")
    print("        cycle 2   :  stage_1 of sort_A")
    print("        cycle 3   :  stage_1 of sort_B")
    print("        ...")
    print("        cycle 108 :  stage_54 of sort_A   ← sort_A done at 110 cy")
    print("        cycle 109 :  stage_54 of sort_B   ← sort_B done at 110 cy")
    print()
    print("        2 sorts / 110 cy  =  1/55 sort/cy   ← SAME as one-at-a-time !")
    print()
    print("    Resource conservation is the bottom line.")
    print()
    print("  ─── Where throughput CAN be recovered (special cases) ─────────────────")
    print()
    print("    1) SUB-TILE PARALLEL SORTS (lane-level parallelism) :")
    print()
    print("       The hardware has 1024 byte-lanes wide. A sort of N elements")
    print("       (N < 1024) only uses N lanes ; the rest are idle. Pack")
    print("       multiple parallel sorts into one pass :")
    print()
    print("           parallel sorts   each sort     stages    throughput")
    print("           in 1024 lanes    size N        used      (sorts/cy)")
    print("           ──────────────   ──────────    ──────    ───────────")
    print("           1                1024            55        1/55")
    print("           2                 512            45        2/45  = 0.044")
    print("           4                 256            36        4/36  = 0.111")
    print("           8                 128            28        8/28  = 0.286")
    print("           16                 64            21       16/21  = 0.762")
    print("           32                 32            15       32/15  = 2.13 *")
    print()
    print("           * 32× small sorts of N=32 actually exceed full-pipeline")
    print("             single-N=1024 throughput, because total work shrinks")
    print()
    print("    2) WIDE FORMATS skip leading stages (TOP truncation) :")
    print()
    print("           format   N (full tile)   stages used   throughput")
    print("           ──────   ─────────────   ───────────   ──────────")
    print("           FP8      1024            55            1/55")
    print("           FP16      512            45            1/45")
    print("           FP32      256            36            1/36")
    print("           FP64      128            28            1/28")
    print()
    print("       Wider formats need fewer comparisons, so each sort takes fewer")
    print("       cycles even on the same time-muxed hardware.")
    print()
    print("    3) BOTTOM TRUNCATION for partial / sub-tile sorts (already in")
    print("       --sort-network and --time-muxed-sort sequencer) reduces stage")
    print("       count further when only K parallel sub-sorts of N/K each")
    print("       are needed.")
    print()
    print("  ─── Why this is OK for DL workloads ───────────────────────────────────")
    print()
    print("    sort frequency in typical DL :")
    print()
    print("       inference  : 0-1 sort per output token  (top-K sampling, beam)")
    print("       training   : 0 sorts per microbatch     (no sort in fwd/bwd)")
    print("       attention  : 0 sorts (just softmax = ROW_MAX + ROW_SUM_EXP)")
    print("       layernorm  : 0 sorts (mean, var = REDUCE)")
    print()
    print("    Even for sort-heavy decode (worst case 1 sort/token, 50 tokens/sec):")
    print()
    print("       sort cost time-muxed : 50 × 55 cy × 1 ns = 2.75 µs / sec")
    print("       fraction of 1 sec    : 0.000275 %")
    print()
    print("    Save 1.3 M gates for a 0.000275 % perf hit.  Trivial trade.")
    print()
    print("  ─── Throughput / latency comparison in absolute time ──────────────────")
    print()
    print("    Suppose 1 cycle = 1 ns. 1024 sorts back-to-back :")
    print()
    print("       option A (full pipe)    : 55 cy fill + 1024 cy steady = 1079 cy")
    print("       option B (time-muxed)   : 1024 × 55 cy                = 56320 cy")
    print()
    print("    Sort-heavy code is 52× slower with option B. But typical DL :")
    print()
    print("       option A : 1 sort per inference call             →  55 cy / 100 ms")
    print("                                                            (negligible)")
    print("       option B : 1 sort per inference call             →  55 cy / 100 ms")
    print("                                                            (identical !)")
    print()
    print("    Picking B is correct iff your workload doesn't sort > 1/55 of the")
    print("    total cycles. Almost all DL workloads pass this bar with margin.")
    print()
    print("  ─── Option D : reuse REDUCE L0 cells (most aggressive) ────────────────")
    print()
    print("    REDUCE tree's L0 already has 512 CMP_SWAP cells. Add :")
    print()
    print("       • permute network upstream of L0    : ~50 K gates")
    print("       • input-mode MUX (reduce vs sort)   :  ~5 K gates")
    print("       • sort sequencer + direction logic  : ~10 K gates")
    print("       • cell output 2-port mode           :  ~3 K gates")
    print("       (CMP_SWAP for sort outputs both values ; for reduce")
    print("        only the winning value)")
    print("       ─────────────────────────────────────────────────")
    print("       TOTAL incremental cost              : ~70 K gates")
    print()
    print("    Saves another ~15 K gates over option B (no separate cell array).")
    print()
    print("    Trade-off : SORT and REDUCE share L0 cells → cannot run concurrent.")
    print("    Acceptable for sort (independent op, 55 cy duration).")
    print()
    print("  ─── Final recommendation ──────────────────────────────────────────────")
    print()
    print("    PRIMARY :  option B (1-stage time-muxed standalone)  ~85 K gates")
    print("               • clean modular block (own permute, own cells)")
    print("               • sort and reduce can run concurrently if needed")
    print("               • simple to verify, simple to floorplan")
    print()
    print("    AGGRESSIVE :  option D (reuse REDUCE L0)              ~70 K gates")
    print("               • saves another 15 K gates")
    print("               • introduces reduce-sort interlock")
    print("               • use only if area extremely tight")
    print()
    print("    AVOID :  option A (full pipeline)  unless throughput-bound")
    print("    AVOID :  option C unless the workload genuinely needs 5-10× sort tput")
    print()
    print("  ─── Updated recommendation table ──────────────────────────────────────")
    print()
    print("    item                          OLD recommendation    NEW recommendation")
    print("    ───────────────────────────   ───────────────────   ───────────────────")
    print("    Sort impl                     full pipeline         1-stage time-muxed")
    print("    Sort cells                    28 K                  512  (54× fewer)")
    print("    Sort gates                    ~1.4 M                ~85 K  (16× fewer)")
    print("    Sort latency (1 sort)         55 cy                 55 cy  (same)")
    print("    Sort throughput               1 / cy                1 / 55 cy")
    print("    Sort scratch                  external 4 KB         RFS_A (reused)")
    print()
    print("  Combined with the unary-REDUCE revision, total VEC-area savings")
    print("  vs. the original (binary-REDUCE + full-pipeline-sort) design :")
    print()
    print("       reduce tree halves   :  −350 K gates")
    print("       sort time-muxed      :  −1300 K gates")
    print("       acc bank in RFS_B    :   −50 K gates")
    print("       ACC_SPILL grows      :   +43 K gates")
    print("       ───────────────────────────────────────")
    print("       NET                  :  ~−1.66 M gates")
    print()
    print("       ≈ HALF of the original VEC-unit area.")
    print()


# ---------------------------------------------------------------------------
# REVISION : Unary-REDUCE + RFS_B-as-acc + half-width tree
# ---------------------------------------------------------------------------

def print_unary_reduce_revision() -> None:
    print(hbar())
    print("  REVISED  ACCUMULATOR + TREE  DESIGN")
    print("  Unary REDUCE  +  RFS_B-as-acc  +  half-width (512 B) tree")
    print(hbar())
    print()
    print("  This section SUPERSEDES parts of --accumulator-design (Q1) and")
    print("  --operand-layout. The earlier sections analysed the case where the")
    print("  REDUCE instruction class is binary (consumes operand A AND B); here")
    print("  we revisit the design under the simpler hypothesis :")
    print()
    print("        REDUCE-class instructions are UNARY (operand A only).")
    print()
    print("  ─── Key observation that triggers the revision ────────────────────────")
    print()
    print("    Worst-case accumulator footprint EQUALS operand-B RF size :")
    print()
    print("        ROW_SUM_EXP, C=2, E_in=1 (FP8), E_out=2 (FP16, double-prec) :")
    print("            R       = 4096 / (C · E_in) = 4096 / 2 = 2048")
    print("            acc_max = R · E_out         = 2048 · 2 = 4096 B = 4 KB")
    print()
    print("        RFS_B size  = 4 KB")
    print()
    print("    → perfect match. The 4 KB constraint applies to (input tile,")
    print("       output tile, and worst-case acc) all by the same arithmetic.")
    print("       So RFS_B is SIZED to be the natural acc home.")
    print()
    print("  ─── The proposal : 4 linked architectural changes ─────────────────────")
    print()
    print("    1.  REDUCE-class instructions are UNARY :")
    print("           VREDUCE_SUM A_src               → result in RFS_B")
    print("           VROW_SUM_EXP A_src              → result in RFS_B")
    print("           VARGMAX A_src                   → result in RFS_B")
    print("           VTOPK_K_LE_4 A_src              → result in RFS_B")
    print("        Encoding : 1 src register only, dest is implicitly RFS_B.")
    print()
    print("    2.  RFS_B is REPURPOSED as accumulator during REDUCE :")
    print("        - reduce-phase : acc lives at RFS_B[0 .. R·E_out − 1]")
    print("        - broadcast-phase : entire RFS_B holds the broadcast output")
    print()
    print("    3.  Reduce-tree input WIDTH halves :  1024 B → 512 B")
    print("        - L0 cells : 512 → 256 (half)")
    print("        - depth (FP8 worst) : 10 → 9 (one level shorter)")
    print("        - cell library and per-cell architecture unchanged")
    print()
    print("    4.  Dedicated acc-FF bank from --accumulator-design Q1 is REMOVED.")
    print("        RFS_B's existing FFs absorb the role.")
    print("        ACC_SPILL latch stays (now sized to handle the read-before-")
    print("         write hazard of broadcast overwriting RFS_B).")
    print()
    print("  ─── Why the original conflicts dissolve ───────────────────────────────")
    print()
    print("    The earlier --accumulator-design Q1 argued for SEPARATE acc FFs")
    print("    based on three conflicts. With unary REDUCE, all three vanish :")
    print()
    print("    conflict (old)            new resolution")
    print("    ───────────────────────   ──────────────────────────────────────────")
    print("    Read-port conflict :      REDUCE never reads RFS_B as operand B.")
    print("      RFS_B can't serve       Only RMW acc port is active. NO conflict.")
    print("      binary-op B AND RMW")
    print("      acc same cycle")
    print()
    print("    3-src FMA conflict :      VFMA is BINARY class (reads A, B, C from")
    print("      C-operand from RFS_B    RFS_A, RFS_B, etc.). REDUCE is unary.")
    print("      vs RMW                  They are different instructions, never")
    print("                              dispatch in the same cycle to the same")
    print("                              ALU pipeline.")
    print()
    print("    Op-to-op pipelining :     op_n (REDUCE) writes broadcast result to")
    print("      op_n leaves acc in      RFS_B. op_{n+1} (BINARY) reads RFS_B as")
    print("      RFS_B → op_{n+1}        operand B = the broadcast result. This")
    print("      can't use RFS_B → bubble  is THE intended dataflow. No bubble.")
    print()
    print("    Resolution principle : separate INSTRUCTION CLASSES, not separate")
    print("    PHYSICAL RESOURCES. Cheaper and equally clean.")
    print()
    print("  ─── Resource and area accounting ──────────────────────────────────────")
    print()
    print("    resource             OLD design        NEW design        delta")
    print("                        (binary REDUCE)   (unary REDUCE)")
    print("    ─────────────────   ──────────────    ──────────────    ──────────")
    print("    Reduce tree         1024 B in,        512 B in,         −350 K gates")
    print("                        ~700 K gates      ~350 K gates")
    print("    Dedicated acc FF    2 KB / 50 K g     0 (in RFS_B)      −50 K gates")
    print("    ACC_SPILL latch     ~256 B / 7 K g    ~2 KB / 50 K g    +43 K gates")
    print("    RFS_B               4 KB (op B only)  4 KB (dual-role)  0")
    print("    RFS_A               4 KB              4 KB              0")
    print("    ──────────────────────────────────────────────────────  ──────────")
    print("    NET                                                     ~−350 K g")
    print()
    print("    ≈ 25 % of total VEC unit area. Plus :")
    print("       • simpler microcode (REDUCE has 1 src field, not 2)")
    print("       • simpler verification (REDUCE class has narrower input)")
    print("       • no port-arbitration logic between reduce-acc and op-B paths")
    print()
    print("  ─── ACC_SPILL latch sizing trick (reverse-order broadcast) ────────────")
    print()
    print("    Naïve broadcast (forward order) :")
    print("      beat 0 writes RFS_B[0..511] (row 0 broadcast)")
    print("      → overwrites acc[0..511/E_out-1] BEFORE later beats can read")
    print("        them. Must spill ALL acc first → ACC_SPILL = full acc size.")
    print()
    print("    REVERSE-order broadcast :")
    print("      beat 0 writes RFS_B[(R-1)*512..(R)*512]  (LAST row's broadcast)")
    print("      beat 1 writes RFS_B[(R-2)*512..(R-1)*512]")
    print("      ...")
    print("      acc lives at RFS_B[0 .. R·E_out − 1] (the START of RFS_B)")
    print("      → first overlap happens only at beat r* where")
    print("           (R-1-r*)·512 < R·E_out")
    print("        i.e. only the LAST few beats touch acc territory.")
    print()
    print("    Spill latch size needed = bytes of acc that the last few beats")
    print("    will overwrite ≈ R·E_out − (R-r_safe)·512 ≈ 64 B in practice.")
    print()
    print("    Worked example R=256, E_out=8 :")
    print("      acc region = [0 .. 2047]  (2 KB)")
    print("      first overlap at r* = 256 − ⌈2048/512⌉ − 1 = 251")
    print("      beats 0..251 are SAFE (write rows 4..255 of broadcast)")
    print("      beats 252..255 overwrite acc → spill needs 4·E_out = 32 B")
    print()
    print("    ⇒ ACC_SPILL latch shrinks from worst-case 2 KB to ~64 B.")
    print()
    print("  ─── Architecture diagram (revised) ────────────────────────────────────")
    print()
    print("                       RFS_A   (4 KB)")
    print("                          │")
    print("                          ▼ 512 B per beat")
    print("                  ┌──────────────────┐")
    print("                  │  REDUCE TREE      │  ← 512 B input, 9 levels")
    print("                  │  (unary, half      │     (down from 1024 B / 10 lvls)")
    print("                  │   the cells)       │")
    print("                  └────────┬──────────┘")
    print("                           │ K partials at chosen stop level")
    print("                           ▼")
    print("                  ┌──────────────────┐")
    print("                  │ K-WIDE RMW ALU    │◀───┐ same-cycle feedback")
    print("                  │ acc[r] += part[r] │    │")
    print("                  └────────┬──────────┘    │")
    print("                           │               │")
    print("                           ▼               │")
    print("                  ┌────────────────────┐   │")
    print("                  │  RFS_B   (4 KB)     │───┘")
    print("                  │                     │")
    print("                  │  during REDUCE :    │")
    print("                  │     [0 .. R·E_out)  │ ← acc")
    print("                  │     [rest]          │ ← don't-care")
    print("                  │                     │")
    print("                  │  during BROADCAST : │")
    print("                  │     full 4 KB       │ ← broadcast output")
    print("                  └────────┬───────────┘")
    print("                           │ broadcast phase only")
    print("                           ▼")
    print("                  ┌────────────────────┐")
    print("                  │  ACC_SPILL latch    │ ← small (~64 B w/ reverse")
    print("                  │  (read-before-write)│   broadcast trick)")
    print("                  └────────┬───────────┘")
    print("                           │")
    print("                           ▼")
    print("                  ┌────────────────────┐")
    print("                  │  BROADCAST TREE     │ ← also halves to 512 B output")
    print("                  └────────┬───────────┘")
    print("                           │")
    print("                           ▼")
    print("                       RFS_B (overwritten in REVERSE order)")
    print()
    print("  ─── Edge cases and how they are handled ───────────────────────────────")
    print()
    print("    case                              handling")
    print("    ───────────────────────────────   ──────────────────────────────────")
    print("    3-src FMA  (D = A·B + C)          BINARY class. RFS_B serves as")
    print("                                       C operand normally. Doesn't run")
    print("                                       concurrent with REDUCE → no issue.")
    print()
    print("    Dot product (Σ A_k · B_k)         2-instruction sequence :")
    print("                                         VFMUL  RFA_A ← A · B")
    print("                                         VREDUCE_SUM  acc ← RFA_A")
    print("                                       (per-lane FMA writes RFA_A,")
    print("                                        REDUCE then consumes it)")
    print()
    print("    REDUCE of a REDUCE result          Result of op_n is in RFS_B. To")
    print("                                       reduce it again, copy first :")
    print("                                         VMOV  RFS_A ← RFS_B")
    print("                                         VREDUCE  ...")
    print("                                       Rare pattern, 1-cycle copy is OK.")
    print()
    print("    GEMM per-element accumulation     Untouched. Per-lane FMA + RFA_A/B")
    print("                                       handle this completely outside")
    print("                                       the reduce-tree pipeline.")
    print()
    print("    Reduce + binary chain :           op_n (REDUCE) writes RFS_B with")
    print("      x = REDUCE(a)                    broadcast result. op_{n+1}")
    print("      y = op(x, c)                     (BINARY) reads RFS_B as B,")
    print("                                       gets x. Natural dataflow, no")
    print("                                       bubble.")
    print()
    print("  ─── Updated recommendation table ──────────────────────────────────────")
    print()
    print("    item                              OLD recommendation   NEW recommendation")
    print("    ───────────────────────────────   ──────────────────   ──────────────────")
    print("    REDUCE instruction class          binary (A and B)     UNARY (A only)")
    print("    Acc storage                       separate 2 KB FF     REUSE RFS_B")
    print("    Reduce-tree input width           1024 B (A | B)       512 B (A only)")
    print("    Operand-A vs B layout             PARALLEL `[A | B]`   N/A (B not used)")
    print("    L0 pair_mode bit                  needed for cross-AB  removed")
    print("    ACC_SPILL latch                   ~256 B               ~64 B with")
    print("                                                            reverse-broadcast")
    print("                                                            trick")
    print("    3-src FMA support                 RFS_B (dual-role)    RFS_B (B or C role)")
    print("    Dot product                       single-instr fused   2-instr sequence")
    print("                                       (binary REDUCE)      (FMUL → REDUCE)")
    print()
    print("  ─── Summary ───────────────────────────────────────────────────────────")
    print()
    print("    By moving complexity from MICROARCHITECTURE (separate physical")
    print("    acc FFs, 1024 B tree, per-cell pair_mode bits) to INSTRUCTION")
    print("    ARCHITECTURE (unary REDUCE class + clean dataflow ordering), we")
    print("    save ~350 K gates AND simplify microcode AND simplify verification.")
    print()
    print("    Cost paid : 1 extra instruction for fused dot-product (cheap),")
    print("                1 extra copy for reduce-of-reduce (rare).")
    print()
    print("    This is the recommended FINAL design.")
    print()


# ---------------------------------------------------------------------------
# TOP-K and ARG-MAX classification : SORT or REDUCE ?
# ---------------------------------------------------------------------------

def print_topk_argmax_design() -> None:
    print(hbar())
    print("  TOP-K and ARG-MAX  :  SORT  or  REDUCE  ?")
    print(hbar())
    print()
    print("  Q : should ARG-MAX and TOP-K go through the SORT network instead")
    print("      of being treated as REDUCE-class operations ?")
    print()
    print("  A : depends per-op.  Most are REDUCE ; only large-K and MEDIAN are")
    print("      genuinely SORT.  Mapping :")
    print()
    print("    op                        nature                        recommended impl")
    print("    ───────────────────────   ──────────────────────────    ────────────────────")
    print("    ARG-MAX  per row/col      reduce (associative)          REDUCE  (augmented")
    print("                              + carry index                  CMP_SWAP cell)")
    print("    TOP-K, K = 1              ≡ ARG-MAX                     REDUCE")
    print("    TOP-K, K ≤ 4              K-pass channeled reduce       REDUCE  (channeled)")
    print("    TOP-K, K > 4              partial sort                  SORT  (if hw exists)")
    print("    MEDIAN                    sort + index N/2              SORT  (mandatory)")
    print("    HISTOGRAM                 scatter + atomic-add          REDUCE + atomics")
    print()
    print("  ─── Why ARG-MAX is naturally a REDUCE ─────────────────────────────────")
    print()
    print("    ARG-MAX is associative :   max(max(a, b), c) = max(a, max(b, c))")
    print("    So it maps to a binary REDUCE tree.  Each CMP_SWAP cell propagates")
    print("    a (value, partial-index) pair upward — value selects, index follows.")
    print()
    print("            level L+1")
    print("             ┌─────────────────────┐")
    print("             │  MAX_BY_VALUE        │  → outputs (max_val, idx_path)")
    print("             └──────────┬──────────┘")
    print("                ┌───────┴───────┐")
    print("        (val_L, idx_L)   (val_R, idx_R)        ← from level L cells")
    print()
    print("    Cell cost : value width E_out  +  partial-index width log₂(subtree)")
    print("    Top-of-tree : full index width log₂(C) — typically 2 B for C ≤ 65 K.")
    print()
    print("    Acc bytes per row :  E_out + log₂(C)/8  ≈  E_out + 2 B")
    print("    Total acc :  R · (E_out + 2)  — at most ~2.5 KB for R=256, FP64.")
    print()
    print("  ─── Why ARG-MAX through SORT is wasteful ──────────────────────────────")
    print()
    print("    For row of N elements :")
    print()
    print("                          REDUCE-tree    SORT (bitonic)    ratio")
    print("                          (ARG-MAX)      (full sort)")
    print("    ────────────────────  ────────────   ──────────────    ────────")
    print("    cells (N=1024)        ~5 K           ~28 K             5.6× more")
    print("    pipe stages           10             55                5.5× longer")
    print("    ALU width per cell    E_out + idx    E_out + idx       same")
    print()
    print("    Sorting an entire row just to read its last element is the same")
    print("    mistake as bubble-sort to find the max — we throw away ordering")
    print("    information we never needed.")
    print()
    print("  ─── TOP-K : channeled REDUCE  vs  SORT  ───────────────────────────────")
    print()
    print("    Channeled REDUCE (K passes through the ARG-MAX reduce tree):")
    print()
    print("        for k in 0 .. K-1 :")
    print("            (val[k], idx[k]) = ROW_ARGMAX(input WHERE mask[*])")
    print("            mask[idx[k]] = 0       # zero out winner for next pass")
    print("        result = top-K (val, idx) pairs per row")
    print()
    print("        latency : K × tree_depth ≈ K · 10 cycles  (for N=1024)")
    print("        acc     : R · K · (E_out + 2) bytes")
    print()
    print("    Bitonic SORT + truncate :")
    print()
    print("        sorted = ROW_BITONIC_SORT(input)")
    print("        result = sorted[:K]   (or sorted[N-K:])")
    print()
    print("        latency : log₂²(N)/2 stages = ~55 cycles for N=1024")
    print("        acc     : K · (E_out + 2) per row, in sort-network scratch")
    print("        cells   : 28 K (for the full sort net, paid once)")
    print()
    print("  ─── Crossover analysis (N=1024 row) ───────────────────────────────────")
    print()
    print("       K     channeled-reduce    sort        winner       ratio")
    print("              cycles              cycles")
    print("       ───   ─────────────────   ─────────   ──────────   ─────")
    print("        1            10            55        REDUCE       5.5×")
    print("        2            20            55        REDUCE       2.8×")
    print("        4            40            55        REDUCE       1.4×")
    print("        5            50            55        ≈ tie        crossover")
    print("        8            80            55        SORT         1.5×")
    print("       16           160            55        SORT         2.9×")
    print("       32           320            55        SORT         5.8×")
    print()
    print("    Caveat : SORT incurs a one-time ~28 K cell cost. If the workload")
    print("    NEVER uses MEDIAN or large-K TOP-K, that 28 K is dead silicon ;")
    print("    in that case force all TOP-K through channeled reduce (cap K ≤ 4")
    print("    in the spec) and skip the sort network entirely.")
    print()
    print("  ─── Implication for acc-FF sizing (Q3 from previous section) ──────────")
    print()
    print("    If we OFFLOAD large-K TOP-K and MEDIAN to the SORT network, the")
    print("    REDUCE-tree's acc footprint stays tame :")
    print()
    print("       op going through REDUCE         acc formula            worst case")
    print("       ────────────────────────────    ───────────────────    ──────────")
    print("       ROW_SUM_EXP / ROW_MAX           R · E_out              ~2 KB")
    print("       ARG-MAX per row                 R · (E_out + 2)        ~2.5 KB")
    print("       TOP-K  (K ≤ 4) channeled        R · K · (E_out + 2)    ~2.5 KB")
    print("                                       (R=64 K=4 FP64)")
    print()
    print("       op going through SORT           acc formula            worst case")
    print("       ────────────────────────────    ───────────────────    ──────────")
    print("       TOP-K  (K > 4)                  K · (E_out + 2)        ~0.3 KB")
    print("                                       per row, in sort scratch")
    print("       MEDIAN                          E_out per row          ~2 KB")
    print("                                       (in sort network)")
    print()
    print("    The 2 KB REDUCE-acc recommendation from Q3 STILL HOLDS when")
    print("    TOP-K large is routed to SORT. Otherwise it would need to grow")
    print("    to ~16 KB to absorb K=8 TOP-K through channeled reduce — strong")
    print("    argument for keeping the SORT network if any large-K op is needed.")
    print()
    print("  ─── Architecture decision tree ────────────────────────────────────────")
    print()
    print("    workload requires :              build SORT net ?    cap on TOP-K's K ?")
    print("    ─────────────────────────────    ────────────────    ──────────────────")
    print("    only sum / max / min / argmax    NO                  K ≤ 4 (channeled)")
    print("    + small-K TOP-K (K ≤ 4)          NO                  K = 4")
    print("    + medium-K TOP-K (K=8..32)       YES (or accept       K ≤ 32 (sort)")
    print("                                     slow channeled)")
    print("    + MEDIAN / sample / sort         YES                  no cap")
    print()
    print("  ─── Why SORT and REDUCE are truly complementary, not redundant ────────")
    print()
    print("    REDUCE answers : 'what is THE winner ?'  (1 element + maybe its idx)")
    print("    SORT   answers : 'what are ALL elements in order ?' (N positions)")
    print()
    print("    ARG-MAX is 1-element-with-idx → REDUCE.")
    print("    Large-K TOP-K is K-positions-with-relative-order → SORT.")
    print("    They are different problem classes, served by different hw.")
    print()


# ---------------------------------------------------------------------------
# Sort network (Batcher bitonic) shared across formats and tile shapes
# ---------------------------------------------------------------------------

def print_sort_network() -> None:
    print(hbar())
    print("  SORT NETWORK  (Batcher bitonic)  — shared across formats & shapes")
    print(hbar())
    print()
    print("  ╔══════════════════════════════════════════════════════════════════╗")
    print("  ║  REVISION NOTE :                                                  ║")
    print("  ║  This section quotes the FULL-PIPELINE area (~28 K cells, 1.4 M  ║")
    print("  ║  gates). The FINAL design uses a TIME-MULTIPLEXED 1-stage variant ║")
    print("  ║  that achieves the same latency at ~85 K gates (16× smaller).    ║")
    print("  ║  The wiring tables, format-tier and shape-tier mechanisms below  ║")
    print("  ║  remain valid for either implementation.                         ║")
    print("  ║  See `python3 vector32_v2.py --time-muxed-sort` for the FINAL     ║")
    print("  ║  recommendation.                                                  ║")
    print("  ╚══════════════════════════════════════════════════════════════════╝")
    print()
    print("  Q : how do we sort tile data inside the VEC unit, with ONE physical")
    print("      network supporting all (shape × format) combinations ?")
    print()
    print("  ─── Why Batcher bitonic ───────────────────────────────────────────────")
    print()
    print("    algorithm           network depth     cells           remarks")
    print("    ─────────────────   ───────────────   ─────────────   ─────────────────")
    print("    bubble sort         N                 N² / 2          too slow / too big")
    print("    Batcher bitonic     k·(k+1) / 2       N·k·(k+1) / 4   *data-independent")
    print("                        (k = log₂ N)                       latency, FIXED")
    print("                                                           butterfly wiring,")
    print("                                                           single CMP_SWAP")
    print("                                                           cell type")
    print("    AKS sorting         O(log N)          O(N log N)      theoretical, huge")
    print("                                                           constants, never")
    print("                                                           built")
    print()
    print("    Bitonic IS the de-facto industry standard for hardware sort.")
    print()
    print("  ─── N=8 example schedule  (6 stages × 4 cells = 24 CMP_SWAP cells) ────")
    print()
    print("    Phase   Stage   byte-d   cells (a↑b = ascending, a↓b = descending)")
    print("    ─────   ─────   ──────   ────────────────────────────────────────────")
    print("    BUILD    S1       1      (0↑1)  (3↓2)  (4↑5)  (7↓6)")
    print("                             ── form 4 alternating ↑↓ pairs of 2 ──")
    print("             S2       2      (0↑2)  (1↑3)  (6↓4)  (7↓5)")
    print("                             ── merge into 4-bitonic groups ──")
    print("             S3       1      (0↑1)  (2↑3)  (5↓4)  (7↓6)")
    print("                             ── clean up within each 4-group ──")
    print("    MERGE    S4       4      (0↑4)  (1↑5)  (2↑6)  (3↑7)")
    print("                             ── cross-half merge into 8-bitonic ──")
    print("             S5       2      (0↑2)  (1↑3)  (4↑6)  (5↑7)")
    print("                             ── halve and merge ──")
    print("             S6       1      (0↑1)  (2↑3)  (4↑5)  (6↑7)")
    print("                             ── final stride-1 clean up ──")
    print()
    print("    Output : i0 ≤ i1 ≤ i2 ≤ i3 ≤ i4 ≤ i5 ≤ i6 ≤ i7")
    print()
    print("  ─── One-stage detail : how a single stage looks (S1, d=1) ─────────────")
    print()
    print("    Each stage is a column of N/2 CMP_SWAP cells operating in parallel.")
    print("    For N=8 stage S1 (distance 1, alternating ↑↓ direction) :")
    print()
    print("              line 0 ──────●───────")
    print("                           │ ↑   <- cell (0,1) ascending : if line0 > line1, swap")
    print("              line 1 ──────●───────")
    print("              line 2 ──────●───────")
    print("                           │ ↓   <- cell (2,3) descending: if line2 < line3, swap")
    print("              line 3 ──────●───────")
    print("              line 4 ──────●───────")
    print("                           │ ↑   <- cell (4,5) ascending")
    print("              line 5 ──────●───────")
    print("              line 6 ──────●───────")
    print("                           │ ↓   <- cell (6,7) descending")
    print("              line 7 ──────●───────")
    print()
    print("    All 4 cells fire in the same cycle ; output of stage S1 then feeds")
    print("    stage S2's input. Other stages have the same shape — only the cell")
    print("    pairing (a, b) and direction change. See the table above for those.")
    print()
    print("  ─── Recursive block structure (Batcher's BUILD-then-MERGE) ────────────")
    print()
    print("    For N=8, the network unrolls from this recursion :")
    print()
    print("    ┌─────────────────────────────────────────────────────────┐")
    print("    │ N=8  bitonic_sort(0..7, ↑)                               │")
    print("    │                                                         │")
    print("    │   ┌─────────────────────┐  ┌─────────────────────┐      │")
    print("    │   │ N=4 sort(0..3, ↑)    │  │ N=4 sort(4..7, ↓)    │      │  PHASE A")
    print("    │   │                     │  │                     │      │  BUILD")
    print("    │   │ ┌───┐ ┌───┐         │  │ ┌───┐ ┌───┐         │      │  bitonic")
    print("    │   │ │ ↑ │ │ ↓ │ <-S1    │  │ │ ↑ │ │ ↓ │ <-S1    │      │  seqs")
    print("    │   │ │0,1│ │2,3│         │  │ │4,5│ │6,7│         │      │")
    print("    │   │ └───┘ └───┘         │  │ └───┘ └───┘         │      │")
    print("    │   │ merge(0..3, ↑) <-S2 │  │ merge(4..7, ↓) <-S2 │      │")
    print("    │   │       <-S3          │  │       <-S3          │      │")
    print("    │   └─────────────────────┘  └─────────────────────┘      │")
    print("    │                                                         │")
    print("    │   ┌──────────────────────────────────────────────┐      │")
    print("    │   │ bitonic_merge(0..7, ↑)                        │      │  PHASE B")
    print("    │   │   S4 d=4 : cross-half (0,4)(1,5)(2,6)(3,7)    │      │  MERGE")
    print("    │   │   S5 d=2 : (0,2)(1,3)(4,6)(5,7)               │      │  into")
    print("    │   │   S6 d=1 : (0,1)(2,3)(4,5)(6,7)               │      │  sorted")
    print("    │   └──────────────────────────────────────────────┘      │")
    print("    └─────────────────────────────────────────────────────────┘")
    print()
    print("    For N=1024 the recursion has 10 levels ; total stages = 55.")
    print("    The cell PAIRINGS at each stage are FIXED by the recursion ;")
    print("    no run-time routing is needed (no Benes, no MUX-permutation).")
    print()
    print("  ─── Stage / cell counts for the 4 native sizes ────────────────────────")
    print()
    print("       N      format   stages  cells/stage   total cells")
    print("       ────   ──────   ──────  ───────────   ───────────")
    print("       1024   FP8        55      512           28160")
    print("        512   FP16       45      256           11520")
    print("        256   FP32       36      128            4608")
    print("        128   FP64       28       64            1792")
    print()
    print("    Build the LARGEST one (N=1024, 28 K cells) physically. Smaller")
    print("    formats reuse it via TOP truncation (skip leading stages).")
    print()
    print("  ─── TOP truncation : format-tier injection (skip front stages) ────────")
    print()
    print("    For a wider format E_in, byte-distance < E_in stages are MEANINGLESS")
    print("    (they would compare bytes inside the same element). Skip those")
    print("    stages — clock-gate them off, route input directly to the first")
    print("    meaningful stage.")
    print()
    print("       format   element     skip stages with     #stages   #stages")
    print("                width E_in  byte-d ∈             skipped   used")
    print("       ──────   ──────────  ──────────────────   ───────   ───────")
    print("       FP8         1 B      (none)                 0         55")
    print("       FP16        2 B      d=1                   10         45")
    print("       FP32        4 B      d=1, d=2              19         36")
    print("       FP64        8 B      d=1, d=2, d=4         27         28")
    print()
    print("    (For N=1024 schedule, 10 phases each contain one d=1 stage,")
    print("     9 phases contain one d=2 stage, 8 phases contain one d=4 stage.)")
    print()
    print("  ─── BOTTOM truncation : sub-tile parallel sorts (skip merge phases) ──")
    print()
    print("    To sort K parallel sub-arrays of N/K elements each, use only the")
    print("    first  log₂(N/K)  phases ; the later phases that MERGE across the")
    print("    K sub-arrays are skipped.")
    print()
    print("       use case (N=1024 worst)     sub-sort   m=log₂   stages used")
    print("                                   shape      sub-N    = m(m+1)/2")
    print("       ─────────────────────────   ────────   ──────   ───────────")
    print("       full tile sort              1 × 1024     10          55")
    print("       8 rows of 128 each          8 × 128       7          28")
    print("       64 rows of 16 each          64 × 16       4          10")
    print("       1024 rows of 1 (trivial)    1024 × 1      0           0")
    print()
    print("    Combined TOP + BOTTOM example :  (8 rows × 32 FP32 elems each)")
    print("       N=256 (FP32) physical schedule : 36 stages")
    print("       BOTTOM cap to 32-elem sub-sort : m=5 → 5·6/2 = 15 stages")
    print("       Plus skip d=1, d=2 (TOP) within those 15 : actually we only")
    print("       count the stages used in the m=5 sub-network with the wider")
    print("       format ⇒ ~14 stages used")
    print()
    print("  ─── Recommended architecture (one shared sort block) ──────────────────")
    print()
    print("       1024 B input bus  (from RFS_A or any RFS)")
    print("              │")
    print("              ▼")
    print("    ┌─────────────────────────────────────────────────────────┐")
    print("    │   FORMAT-TIER INJECT MUX  (TOP truncation)               │")
    print("    │   ── select start-stage based on E_in ──                 │")
    print("    └────────────────┬────────────────────────────────────────┘")
    print("                     │")
    print("                     ▼")
    print("    ┌─────────────────────────────────────────────────────────┐")
    print("    │   BITONIC SORT NETWORK  (28 K CMP_SWAP cells, N_max=1024)│")
    print("    │   55 stages × 512 cells, FIXED butterfly wiring          │")
    print("    │   per-cell : 8 B max-width CMP_SWAP_HI/LO + dir bit      │")
    print("    │   per-stage : 1 b stop-bit (BOTTOM truncation)           │")
    print("    └────────────────┬────────────────────────────────────────┘")
    print("                     │")
    print("                     ▼")
    print("       1024 B output bus  (sorted, back to RFS_A or RFS_B)")
    print()
    print("  ─── What the sort block SHARES with the reduce/broadcast tree ─────────")
    print()
    print("    resource                       reduce tree   sort net   shared ?")
    print("    ────────────────────────────   ───────────   ────────   ──────────")
    print("    CMP_SWAP cell library          ✓ (max/min)   ✓ (main)   ★ same lib")
    print("    ADD cell library               ✓             ✗")
    print("    FANOUT cell                    ✓             ✗")
    print("    INJECT MUX  (format-tier)      ✓             ✓          ★ same ctrl")
    print("    STOP-BIT MUX (shape-tier)      ✓             ✓          ★ same ctrl")
    print("    Per-stage wiring topology      binary tree   bitonic    NO — must")
    print("                                                              be 2 blocks")
    print("    Microcode op decoder           ✓             ✓          ★ same op")
    print("                                                              format")
    print()
    print("    Conclusion : 2 PHYSICAL networks (cannot fold one into the other),")
    print("    but they share the cell library, control electronics, and microcode")
    print("    encoding. A single ALU-cell macro is reused across both.")
    print()
    print("  ─── Total area estimate for combined REDUCE + BROADCAST + SORT ────────")
    print()
    print("    block                                cells     gates")
    print("    ──────────────────────────────────   ───────   ─────────")
    print("    REDUCE/BROADCAST tree (this design)   ~12 K    ~600 K")
    print("    SORT network (FP8 worst case)         ~28 K    ~1.4 M")
    print("    Shared control + spill latches         —       ~200 K")
    print("    ──────────────────────────────────   ───────   ─────────")
    print("    TOTAL                                 ~40 K    ~2.2 M gates")
    print()
    print("    Reference (vector4k_v2 §5 Stage-B core) :")
    print("      128 FMA lanes × 10 K gates each  ≈  1.28 M gates")
    print()
    print("    ⇒  sort network alone ≈ 1× FMA-core area.  Worth adding only if")
    print("        the workload uses sort (top-K attention, median, sparse-")
    print("        intersect, etc.) ; otherwise omit and save ~1.4 M gates.")
    print()


# ---------------------------------------------------------------------------
# Format-tier injection + stop-bit truncation design notes
# ---------------------------------------------------------------------------

def print_format_tier_design() -> None:
    print(hbar())
    print("  FORMAT-TIER INJECTION + STOP-BIT TRUNCATION  (multi-format design)")
    print(hbar())
    print()
    print("  Q : how does ONE physical tree handle FP8 (N=1024), FP16 (N=512),")
    print("      FP32 (N=256), FP64 (N=128) inputs AND arbitrary row widths C ?")
    print()
    print("  A : two ORTHOGONAL truncation knobs, applied at opposite ends of")
    print("      the tree :")
    print()
    print("      ┌────────────────┬────────────────────┬────────────────────────┐")
    print("      │                │  TOP truncation    │  BOTTOM truncation     │")
    print("      ├────────────────┼────────────────────┼────────────────────────┤")
    print("      │ trigger        │  wider E_in        │  narrower row width C  │")
    print("      │ skip which lvl │  first  log2(E_in) │  last  log2(N_max) −   │")
    print("      │                │  stages (leaf side)│  log2(elems/subtree)   │")
    print("      │                │                    │  stages (root side)    │")
    print("      │ mechanism      │  per-stage         │  per-stage             │")
    print("      │                │  INJECT MUX        │  STOP-BIT MUX          │")
    print("      │ example        │  FP32 skips L0,L1  │  (32×32 FP32) → 4 sub- │")
    print("      │                │                    │  trees stop at L7      │")
    print("      └────────────────┴────────────────────┴────────────────────────┘")
    print()
    print("  ─── TOP truncation : format-tier injection ────────────────────────────")
    print()
    print("       1024 B operand-A bus")
    print("            │")
    print("            ├──►[INJECT_L0 MUX]──► L0  (1024 × 1B  FP8  elements)   ← FP8")
    print("            │                          │  stage 0 :")
    print("            │                          ▼  512 × (1B+1B → 2B promote)")
    print("            │                          │")
    print("            ├──►[INJECT_L1 MUX]──► L1  ( 512 × 2B  FP16 elements)   ← FP16")
    print("            │                          │  stage 1 :")
    print("            │                          ▼  256 × (2B+2B → 4B promote)")
    print("            │                          │")
    print("            ├──►[INJECT_L2 MUX]──► L2  ( 256 × 4B  FP32 elements)   ← FP32")
    print("            │                          │  stage 2 :")
    print("            │                          ▼  128 × (4B+4B → 8B promote)")
    print("            │                          │")
    print("            ├──►[INJECT_L3 MUX]──► L3  ( 128 × 8B  FP64 elements)   ← FP64")
    print("            │                          │  stage 3..9 : 8B+8B → 8B")
    print("            │                          ▼  (cap at FP64 width)")
    print("            │                         ...")
    print("            │                          ▼  ROOT")
    print()
    print("    Key design points :")
    print()
    print("      1) Per-stage INJECT MUX : 2:1 select { cascade_from_prev_stage,")
    print("                                              fresh_bus[level_k] }.")
    print("         Select = 1 ⇒ this level is the input ; all earlier stages")
    print("         are clock-gated off.")
    print()
    print("      2) ALU width doubles per stage (1B → 2B → 4B → 8B → cap).  This")
    print("         geometric progression EXACTLY MATCHES the IEEE-754 format")
    print("         widths {FP8, FP16, FP32, FP64}, so every format has a")
    print("         natural injection point.")
    print()
    print("      3) Width promotion is FREE.  FP8 input naturally widens to")
    print("         FP16 after stage 0, FP32 after stage 1, FP64 after stage 2.")
    print("         Optional cast/round unit between stages can cap the width")
    print("         (e.g. cap FP32 output : FP8 → FP32 in 2 stages, then stay")
    print("         FP32 for the remaining stages).")
    print()
    print("      4) Lane mapping is contiguous : bytes [i·E_in, (i+1)·E_in)")
    print("         belong to element i.  Stage-k pair-adders always combine")
    print("         element 2i with element 2i+1, regardless of E_in.")
    print()
    print("      5) Input bus fan-out cost : the 1024 B operand-A bus must")
    print("         drive ALL 4 inject points → +3 buffer stages between")
    print("         injects (≈1 K gates, negligible vs ALU area).")
    print()
    print("      6) Latency varies with format :")
    print("           FP8 : 10 stages,  FP16 : 9,  FP32 : 8,  FP64 : 7")
    print("         If uniform latency is desired, NOP pipeline-regs fill the")
    print("         skipped slots (no ALU, just registers).")
    print()
    print("  ─── BOTTOM truncation : per-row stop-bit ──────────────────────────────")
    print()
    print("    ... ─►─ stage k ALU ─►─ pipeline reg ─►──┬─►─ STOP-BIT MUX ─►─")
    print("                                              │             │")
    print("                                              │             ▼")
    print("                                              │     ACC_SPILL[k_subtree]")
    print("                                              │")
    print("                                              └─►─ to stage k+1")
    print()
    print("    Key design points :")
    print()
    print("      1) Per-stage STOP-BIT MUX : 2:1 select { cascade_to_next_stage,")
    print("                                                 route_to_spill_slot }.")
    print()
    print("      2) Stop-level = inject_level + ⌈log2(elems_per_subtree)⌉")
    print("         Examples :")
    print("           (32,32,FP32) : inject L2, 32 elems/row, stop at L7")
    print("           (64,64,FP8 ) : inject L0, 64 elems/row, stop at L6")
    print()
    print("      3) K parallel sub-trees stop at the SAME stage simultaneously")
    print("         (one beat = one stop level), each routes to its own slot")
    print("         in the K-wide ACC_SPILL latch.")
    print()
    print("      4) Stages past the stop level are clock-gated (saves power on")
    print("         small-row beats).")
    print()
    print("  ─── Per-stage cell architecture (combined INJECT + ALU + STOP) ────────")
    print()
    print("        Fresh input bus slice          Cascade from stage k−1")
    print("                │                              │")
    print("                ▼                              ▼")
    print("          ┌─────────────────────────────────────────┐")
    print("          │   INJECT MUX  (TOP truncation)          │  inject_here[k] (1b)")
    print("          └─────────────────────────────────────────┘")
    print("                              │")
    print("                              ▼")
    print("          ┌─────────────────────────────────────────┐")
    print("          │   E_k-wide  ALU  +  optional CAST       │  opcode      (3b)")
    print("          │   (ADD | CMP_SWAP | PASS | MUX)         │  promote_bit (1b)")
    print("          └─────────────────────────────────────────┘")
    print("                              │")
    print("                              ▼")
    print("                       pipeline register")
    print("                              │")
    print("                              ▼")
    print("          ┌─────────────────────────────────────────┐")
    print("          │   STOP-BIT MUX  (BOTTOM truncation)     │  stop_here[k]   (1b)")
    print("          └──────┬─────────────────────────┬────────┘")
    print("                 │                         │")
    print("                 ▼ cascade                 ▼ stop")
    print("           next stage              ACC_SPILL[k_subtree_id]")
    print()
    print("    Per-stage microcode bits : inject(1) + opcode(3) + promote(1) +")
    print("                               stop(1) = 6 bits / stage")
    print("    Total / beat : 10 stages × 6 b = 60 b microcode word")
    print("                   (fits inside the §5.4 beat-word easily)")
    print()
    print("  ─── Per-stage ALU width and format-injection table ────────────────────")
    print()
    print("    stage   level→level+1   N_elem    E_k width   ALU op        native fmt")
    print("    ─────   ─────────────   ──────    ─────────   ───────────   ──────────")
    print("    inject     → L0          1024       1 B         —           ★ FP8")
    print("       0    L0 → L1     1024 → 512    1B → 2B    1B+1B→2B promo  cascade")
    print("    inject     → L1           512       2 B         —           ★ FP16")
    print("       1    L1 → L2      512 → 256    2B → 4B    2B+2B→4B promo  cascade")
    print("    inject     → L2           256       4 B         —           ★ FP32")
    print("       2    L2 → L3      256 → 128    4B → 8B    4B+4B→8B promo  cascade")
    print("    inject     → L3           128       8 B         —           ★ FP64")
    print("       3    L3 → L4      128 →  64    8B (cap)   8B+8B→8B        cascade")
    print("       4    L4 → L5       64 →  32    8B         8B+8B→8B        cascade")
    print("       5    L5 → L6       32 →  16    8B         8B+8B→8B        cascade")
    print("       6    L6 → L7       16 →   8    8B         8B+8B→8B        cascade")
    print("       7    L7 → L8        8 →   4    8B         8B+8B→8B        cascade")
    print("       8    L8 → L9        4 →   2    8B         8B+8B→8B        cascade")
    print("       9    L9 → L10       2 →   1    8B         8B+8B→8B        ROOT")
    print()
    print("  ─── Worked examples (4 formats × multiple row widths) ─────────────────")
    print()
    print("    input tile         inject  subtree   stop   #skipTOP  #skipBOT  active")
    print("    ─────────────────  ──────  ───────   ────   ────────  ────────  ──────")
    print("    (1,  1024, FP8 )   L0      128 elem  L7        0         3        7")
    print("    (1,  1024, FP32)   L2      128 elem  L9        2         1        7")
    print("    (8,   128, FP32)   L2      128 elem  L9        2         1        7")
    print("    (32,   32, FP32)   L2       32 elem  L7        2         3        5")
    print("    (64,   64, FP8 )   L0       64 elem  L6        0         4        6")
    print("    (8,   256, FP16)   L1      256 elem  L9        1         1        8")
    print("    (8,    64, FP64)   L3       64 elem  L9        3         1        6")
    print("    (256,   4, FP32)   L2        4 elem  L4        2         6        2  ← extreme")
    print()
    print("    The last case ((256, 4, FP32) — 4-elem rows) uses only 2 of 10")
    print("    pipeline stages : 80 % of the tree is clock-gated this beat.")
    print()
    print("  ─── Area cost vs alternatives ─────────────────────────────────────────")
    print()
    print("    scheme                                         relative ALU area")
    print("    ─────────────────────────────────────────      ─────────────────")
    print("    4 independent trees (one per FP{8,16,32,64})         4.0 ×")
    print("    1 byte-granular tree + carry-chain configure         ~1.3 ×  *slow")
    print("    1 format-tier tree (this design)                     ~1.5 ×")
    print("    1 single-format tree (FP32 only, no flexibility)      1.0 ×")
    print()
    print("    Why 1.5× and not 1.0× for the format-tier scheme :")
    print("      • per-stage ALU width doubles (1B → 8B), but element count")
    print("        halves, so the per-stage byte count is ~constant ≈ 1024 B")
    print("      • 10 stages × ~1024 B of ALU logic ≈ 10 KB of arithmetic,")
    print("        ≈ 1.5× a single FP32-wide tree (which is 8 stages × 1024 B)")
    print("      • the +50 % buys :  4 formats × any row width support,")
    print("                         + free width promotion for ROW_SUM_EXP-style")
    print("                           narrow→wide accumulation,")
    print("                         + reuse for sort / merge_sort (just opcodes).")
    print()


def print_pseudo_code() -> None:
    print(hbar())
    print("  PSEUDO-CODE  : row_sum_exp(in_spec, double_width=False)")
    print(hbar())
    print("""
  Inputs:
    in_spec      : TileSpec(R, C, fmt_in)        # R · C · E_in = 4096
    double_width : bool                          # if True : E_out = 2·E_in
                                                 #          C_out = C / 2
  Output:
    out_spec     : TileSpec(R, C_out, fmt_out)   # R · C_out · E_out = 4096
    D[r, c]      = Σ_{c'=0..C-1} A[r, c']        # row sum cast to E_out,
                                                 # broadcast across all C_out cols.

  Hardware:
    RFS_A          : 4 KB,  holds input tile A.
    RFS_B          : 4 KB,  REPURPOSED.
                       phase 1 (reduce)    :
                         RFS_B[0 : R·E_out] holds R partial sums (the acc[]).
                         The remaining (4096 − R·E_out) bytes are don't-care.
                       phase 2 (broadcast) :
                         RFS_B is fully overwritten with the output tile D.
                       NOTE : original operand-B contents are DESTROYED.
    REDUCE_TREE    : 1024 B-input Benes-like front half (log2(N_in) ADD levels).
    BROADCAST_TREE : 1024 B-output Benes-like back half (log2(N_out) FANOUT levels).
    ACC_SPILL      : R · E_out byte latch. Reads RFS_B[0:R·E_out] in ONE cycle
                     between phase 1 and phase 2 — resolves the read-after-write
                     hazard between phase-1 acc[] and phase-2 D[].
                     ≤ 1 KB for all canonical shapes; physically a small SRAM
                     or DFF cluster sitting next to the broadcast tree input.

  Algorithm:

  ═══ PHASE 1 — REDUCE  (NUM_BEATS = 8 cycles : read entire RFS_A) ═══

      RFS_B[0 : R·E_out] ← 0                                  # zero acc[] slots

      for beat in 0 .. 7:
          A_beat ← RFS_A[beat·512 : beat·512 + 512]           # one tilelet

          if regime A (row_bytes_in ≤ 512):
              K = rows_per_input_beat                         # = 512 / row_bytes_in
              for k in 0 .. K-1:
                  row_id  = beat·K + k
                  elems   = A_beat's k-th row segment         # C elems × E_in
                  partial = REDUCE_TREE(elems, op=ADD,
                                        in_fmt=E_in, out_fmt=E_out,
                                        depth=ceil(log2(C)))
                  acc     = RFS_B[row_id·E_out : (row_id+1)·E_out]
                  RFS_B[row_id·E_out : (row_id+1)·E_out] ← acc + partial

          else (regime B, row_bytes_in > 512):
              row_id      = beat // beats_per_row_in
              beat_in_row = beat %  beats_per_row_in
              elems       = A_beat's elements                  # 512/E_in × E_in
              partial     = REDUCE_TREE(elems, op=ADD,
                                        in_fmt=E_in, out_fmt=E_out,
                                        depth=ceil(log2(512/E_in)))
              acc         = RFS_B[row_id·E_out : (row_id+1)·E_out]
              RFS_B[row_id·E_out : (row_id+1)·E_out] ← acc + partial
              # row_id is fully reduced when beat_in_row == beats_per_row_in - 1


  ═══ INTERLUDE — ACC_SPILL (1 cycle, hides hazard) ═══

      ACC_SPILL ← RFS_B[0 : R·E_out]                          # latch acc[]


  ═══ PHASE 2 — EXPAND  (NUM_BEATS = 8 cycles : write entire RFS_B) ═══

      for beat in 0 .. 7:
          D_beat = empty 512 B buffer

          if regime out A (row_bytes_out ≤ 512):
              K = rows_per_output_beat                        # = 512 / row_bytes_out
              for k in 0 .. K-1:
                  row_id = beat·K + k
                  sum_r  = ACC_SPILL[row_id]                  # 1 elem of E_out
                  copies = BROADCAST_TREE(sum_r, fanout=C_out,
                                          out_fmt=E_out)      # C_out × sum_r
                  D_beat's k-th row segment ← copies

          else (regime out B, row_bytes_out > 512):
              row_id      = beat // beats_per_row_out
              sum_r       = ACC_SPILL[row_id]
              copies      = BROADCAST_TREE(sum_r, fanout=512/E_out, out_fmt=E_out)
              D_beat      ← copies                           # 512/E_out copies

          RFS_B[beat·512 : beat·512 + 512] ← D_beat


  ═══ Total cycle count ═══
      Reduce      :  8 cy
      ACC_SPILL   :  1 cy   (single-cycle wide read + latch)
      Broadcast   :  8 cy
      ─────────────────
      Total       : 17 cy   (throughput = 1 ROW_SUM_EXP / 17 cy)
""")


# ---------------------------------------------------------------------------
# Beat-by-beat trace
# ---------------------------------------------------------------------------

def print_plan_header(plan: RowSumExpPlan) -> None:
    promo = "(double-width promoted, E_out = 2·E_in)" \
            if plan.E_out == 2 * plan.E_in else "(same precision as input)"
    print(f"  E_in              : {plan.E_in} B/elem  ({plan.in_spec.fmt})")
    print(f"  E_out             : {plan.E_out} B/elem  ({plan.out_spec.fmt})  {promo}")
    print(f"  row_bytes_in      : {plan.row_bytes_in} B  "
          f"= C·E_in = {plan.in_spec.C}·{plan.E_in}")
    print(f"  row_bytes_out     : {plan.row_bytes_out} B  "
          f"= C_out·E_out = {plan.out_spec.C}·{plan.E_out}")
    print(f"  regime_in         : {plan.regime_in}  ", end="")
    if plan.regime_in == "A":
        print(f"({plan.rows_per_input_beat} full rows per input beat)")
    else:
        print(f"(input row spans {plan.beats_per_row_in} beats)")
    print(f"  regime_out        : {plan.regime_out}  ", end="")
    if plan.regime_out == "A":
        print(f"({plan.rows_per_output_beat} full rows per output beat)")
    else:
        print(f"(output row spans {plan.beats_per_row_out} beats)")
    print(f"  reduce beats      : {plan.num_reduce_beats}")
    print(f"  broadcast beats   : {plan.num_broadcast_beats}")
    print(f"  total cycles      : {plan.num_reduce_beats + 1 + plan.num_broadcast_beats}"
          f"   (8 reduce + 1 ACC_SPILL + 8 broadcast)")
    print(f"  acc storage       : {plan.acc_bytes} B  in RFS_B[0..{plan.acc_bytes-1}]"
          f"   (R · E_out = {plan.in_spec.R} · {plan.E_out})")
    print(f"  ACC_SPILL latch   : {plan.acc_bytes} B  "
          f"(={'fits in 1 tilelet' if plan.acc_bytes <= TILELET_BYTES else 'larger than tilelet — needs deeper SRAM'})")
    print(f"  reduce sub-tree   : {plan.reduce_elems_per_subtree} {plan.in_spec.fmt} leaves "
          f"→ {plan.reduce_tree_levels} ADD levels   (×{plan.reduce_subtrees_per_beat} sub-trees per beat)")
    print(f"  bcast  sub-tree   : 1 {plan.out_spec.fmt} seed → "
          f"{plan.broadcast_tree_levels} FANOUT levels → {plan.broadcast_elems_per_subtree} {plan.out_spec.fmt} copies"
          f"   (×{plan.broadcast_subtrees_per_beat} sub-trees per beat)")
    print()


def print_reduce_beats(plan: RowSumExpPlan) -> None:
    R, C = plan.in_spec.R, plan.in_spec.C
    E_in, E_out = plan.E_in, plan.E_out
    fmt_in, fmt_out = plan.in_spec.fmt, plan.out_spec.fmt
    depth = plan.reduce_tree_levels

    if plan.regime_in == "A":
        rpb = plan.rows_per_input_beat
        for beat in range(NUM_BEATS):
            row_start = beat * rpb
            row_end = row_start + rpb
            t0 = beat * TILELET_BYTES
            t1 = t0 + TILELET_BYTES - 1
            print(f"    Beat {beat}  (cycle {beat})")
            print(f"      A_tilelet     : RFS_A[{t0}..{t1}]   "
                  f"= {rpb} rows × {C} {fmt_in} elements  "
                  f"= rows {row_start}..{row_end-1}")
            print(f"      Tree input    : {TILELET_BYTES // E_in} {fmt_in} leaves  "
                  f"→ split into {rpb} parallel sub-trees of {C} elems each")
            print(f"      Tree levels   : {depth} ADD levels per sub-tree  "
                  f"(within-row reduce)")
            print(f"      Tree output   : {rpb} {fmt_out} partial sums "
                  f"(cast {fmt_in} → {fmt_out})")
            print(f"      Acc fetch     : RFS_B[{row_start*E_out}..{row_end*E_out-1}]  "
                  f"= acc[{row_start}..{row_end-1}]")
            print(f"      RMW (ADD)     : acc[{row_start}..{row_end-1}] += partial[0..{rpb-1}]")
            print(f"      Acc store     : RFS_B[{row_start*E_out}..{row_end*E_out-1}] ← acc'")
            print()
    else:  # regime B — input row spans multiple beats
        bpr = plan.beats_per_row_in
        elems_per_beat = TILELET_BYTES // E_in
        for beat in range(NUM_BEATS):
            row_id = beat // bpr
            beat_in_row = beat % bpr
            t0 = beat * TILELET_BYTES
            t1 = t0 + TILELET_BYTES - 1
            done = "   ★ row fully reduced after this beat" \
                   if beat_in_row == bpr - 1 else ""
            print(f"    Beat {beat}  (cycle {beat})")
            print(f"      A_tilelet     : RFS_A[{t0}..{t1}]   "
                  f"= segment {beat_in_row+1}/{bpr} of row {row_id}  "
                  f"= {elems_per_beat} {fmt_in} elements")
            print(f"      Tree input    : 1 sub-tree of {elems_per_beat} leaves")
            print(f"      Tree levels   : {depth} ADD levels  (full-segment reduce)")
            print(f"      Tree output   : 1 {fmt_out} partial sum")
            print(f"      Acc fetch     : RFS_B[{row_id*E_out}..{(row_id+1)*E_out-1}]  "
                  f"= acc[{row_id}]")
            print(f"      RMW (ADD)     : acc[{row_id}] += partial{done}")
            print(f"      Acc store     : RFS_B[{row_id*E_out}..{(row_id+1)*E_out-1}]")
            print()


def print_acc_spill(plan: RowSumExpPlan) -> None:
    print(sub())
    print(f"    ACC_SPILL  (cycle {NUM_BEATS})")
    print(sub())
    print(f"      latch ← RFS_B[0..{plan.acc_bytes-1}]    "
          f"({plan.acc_bytes} B = R·E_out)")
    print(f"      → wide single-cycle read; resolves the read-after-write hazard")
    print(f"        that would otherwise occur in phase 2 (which overwrites RFS_B).")
    print()


def print_broadcast_beats(plan: RowSumExpPlan) -> None:
    R, C_out = plan.out_spec.R, plan.out_spec.C
    E_out = plan.E_out
    fmt_out = plan.out_spec.fmt
    depth = plan.broadcast_tree_levels

    if plan.regime_out == "A":
        rpb = plan.rows_per_output_beat
        for beat in range(NUM_BEATS):
            row_start = beat * rpb
            row_end = row_start + rpb
            t0 = beat * TILELET_BYTES
            t1 = t0 + TILELET_BYTES - 1
            cy = beat + NUM_BEATS + 1
            print(f"    Beat {beat + NUM_BEATS}  (cycle {cy})")
            print(f"      Acc read      : ACC_SPILL[{row_start}..{row_end-1}]  "
                  f"= {rpb} {fmt_out} sums")
            print(f"      Tree input    : {rpb} {fmt_out} elements (1 per row)")
            print(f"      Tree levels   : {depth} FANOUT levels per sub-tree  "
                  f"(broadcast within row)")
            print(f"      Tree output   : {rpb} rows × {C_out} {fmt_out} copies "
                  f"= {rpb * C_out * E_out} B = {TILELET_BYTES} B")
            print(f"      RFS_B write   : RFS_B[{t0}..{t1}] ← broadcast tilelet  "
                  f"(D rows {row_start}..{row_end-1})")
            print()
    else:  # regime out B — output row spans multiple beats
        bpr = plan.beats_per_row_out
        elems_per_beat = TILELET_BYTES // E_out
        for beat in range(NUM_BEATS):
            row_id = beat // bpr
            beat_in_row = beat % bpr
            t0 = beat * TILELET_BYTES
            t1 = t0 + TILELET_BYTES - 1
            cy = beat + NUM_BEATS + 1
            print(f"    Beat {beat + NUM_BEATS}  (cycle {cy})")
            print(f"      Acc read      : ACC_SPILL[{row_id}]  = 1 {fmt_out} sum")
            print(f"      Tree input    : 1 {fmt_out} element")
            print(f"      Tree levels   : {depth} FANOUT levels  "
                  f"(broadcast within tilelet)")
            print(f"      Tree output   : {elems_per_beat} {fmt_out} copies "
                  f"= {TILELET_BYTES} B")
            print(f"      RFS_B write   : RFS_B[{t0}..{t1}] ← broadcast tilelet  "
                  f"(D row {row_id}, segment {beat_in_row+1}/{bpr})")
            print()


def print_beat_trace(plan: RowSumExpPlan) -> None:
    print(hbar())
    print(f"  EXAMPLE  : ROW_SUM_EXP   in = ({plan.in_spec})   "
          f"out = ({plan.out_spec})")
    print(hbar())
    print_plan_header(plan)

    print(sub())
    print("    PHASE 1 — REDUCE   (8 cycles, reads entire RFS_A)")
    print(sub())
    print()
    print_reduce_beats(plan)
    print_acc_spill(plan)

    print(sub())
    print("    PHASE 2 — EXPAND   (8 cycles, overwrites entire RFS_B)")
    print(sub())
    print()
    print_broadcast_beats(plan)


# ---------------------------------------------------------------------------
# Default examples
# ---------------------------------------------------------------------------

DEFAULT_EXAMPLES: List[Tuple[TileSpec, bool]] = [
    (TileSpec(R=8,  C=128,  fmt='FP32'), False),  # canonical, 1 row / beat
    (TileSpec(R=32, C=32,   fmt='FP32'), False),  # multi-row / beat (4)
    (TileSpec(R=1,  C=1024, fmt='FP32'), False),  # row spans 8 beats
    (TileSpec(R=8,  C=256,  fmt='FP16'), True),   # double-width FP16 → FP32
    (TileSpec(R=64, C=64,   fmt='FP8'),  False),  # many rows / beat (8)
]


# ---------------------------------------------------------------------------
# Interactive helpers
# ---------------------------------------------------------------------------

def _legal_R_values(in_fmt: str) -> List[int]:
    """All powers-of-2 R such that R · E_in divides TILE_BYTES exactly
    AND C = TILE_BYTES / (R · E_in) is itself ≥ 1 and a power of 2."""
    E = FORMAT_E[in_fmt]
    legal: List[int] = []
    R = 1
    while R * E <= TILE_BYTES:
        if TILE_BYTES % (R * E) == 0:
            C = TILE_BYTES // (R * E)
            if C >= 1 and (C & (C - 1)) == 0:    # C is power of 2
                legal.append(R)
        R *= 2
    return legal


def _ask(prompt: str, default: str = "") -> str:
    """Read one line of input, with an optional default if the user hits ENTER."""
    suffix = f" [default {default}]" if default else ""
    s = input(f"  {prompt}{suffix} : ").strip()
    return s if s else default


def interactive_input() -> RowSumExpPlan:
    print()
    print(hbar())
    print("  ROW_SUM_EXP — interactive input")
    print(hbar())
    print(f"  Tile is fixed at {TILE_BYTES} B, so R · C · E_in = {TILE_BYTES}.")
    print(f"  You provide  R  and  input format ;  C is computed automatically.")
    print(f"  Then choose  output format  (must equal input or its double-width).")
    print(f"  Supported formats : {', '.join(FORMAT_E.keys())}")
    print()

    # ---- input format ----
    fmt_choices = list(FORMAT_E.keys())
    while True:
        s = _ask(f"input  data format ({'/'.join(fmt_choices)})",
                 default="FP32").upper()
        if s in FORMAT_E:
            in_fmt = s
            break
        print(f"    invalid format {s!r}; choose one of {fmt_choices}")

    # ---- R ----
    legal = _legal_R_values(in_fmt)
    print(f"  legal R for {in_fmt} : {legal}")
    while True:
        s = _ask("R (rows)", default=str(legal[len(legal) // 2]))
        try:
            R = int(s)
            if R not in legal:
                print(f"    R={R} not in legal set {legal}")
                continue
            break
        except ValueError:
            print(f"    invalid R={s!r}; please enter an integer")

    # ---- compute C ----
    E_in = FORMAT_E[in_fmt]
    C = TILE_BYTES // (R * E_in)
    print(f"  → C = {TILE_BYTES} / (R · E_in) = {TILE_BYTES} / ({R} · {E_in}) "
          f"= {C}")
    print(f"  → input tile = {R} × {C} {in_fmt}")

    # ---- output format ----
    same = in_fmt
    promo = NEXT_FORMAT.get(in_fmt)
    legal_out = [same] + ([promo] if promo else [])
    while True:
        s = _ask(f"output data format ({'/'.join(legal_out)})",
                 default=same).upper()
        if s == in_fmt:
            double_width = False
            break
        if s == promo:
            if C % 2 != 0:
                print(f"    cannot promote: C={C} not divisible by 2; "
                      f"choose {in_fmt} or change R.")
                continue
            double_width = True
            break
        print(f"    output format must be {in_fmt} (same) "
              f"or {promo} (double-width); got {s!r}")

    in_spec = TileSpec(R=R, C=C, fmt=in_fmt)
    return make_plan(in_spec, double_width=double_width)


# ---------------------------------------------------------------------------
# CLI
# ---------------------------------------------------------------------------

def parse_args() -> argparse.Namespace:
    p = argparse.ArgumentParser(
        description=("VEC-4K-v2 datapath analyser + ROW_SUM_EXP microcode "
                     "generator (per vector4k_v2.md)."),
    )
    p.add_argument("--tile", nargs=3, metavar=("R", "C", "FMT"),
                   help="non-interactive: generate microcode trace for ONE "
                        "tile shape (e.g. --tile 8 128 FP32)")
    p.add_argument("--double-width", action="store_true",
                   help="with --tile or interactive input, promote output "
                        "element format to next-higher precision "
                        "(E_out = 2·E_in, C_out = C/2)")
    p.add_argument("--with-context", action="store_true",
                   help="also print overview + tree topology + Benes diagram "
                        "+ pseudo-code before the beat trace")
    p.add_argument("--diagram", action="store_true",
                   help="print only the detailed back-to-back butterfly tree "
                        "diagram and exit")
    p.add_argument("--format-tier", action="store_true",
                   help="print only the multi-format injection + stop-bit "
                        "design notes and exit")
    p.add_argument("--benes-analysis", action="store_true",
                   help="print only the analysis of whether Benes routing is "
                        "actually needed (spoiler: no) and exit")
    p.add_argument("--operand-layout", action="store_true",
                   help="print only the operand-A vs accumulator path layout "
                        "analysis (parallel vs interleaved on the 1024 B bus)")
    p.add_argument("--sort-network", action="store_true",
                   help="print only the sort-network design notes "
                        "(Batcher bitonic, shared across formats and shapes)")
    p.add_argument("--accumulator-design", action="store_true",
                   help="print only the accumulator FF design analysis "
                        "(shared vs separate, feedback path, worst-case sizing)")
    p.add_argument("--topk-argmax", action="store_true",
                   help="print only the TOP-K / ARG-MAX classification analysis "
                        "(SORT vs REDUCE — when each wins)")
    p.add_argument("--unary-reduce", action="store_true",
                   help="print only the REVISED design : unary REDUCE class, "
                        "RFS_B as accumulator, half-width (512 B) tree. This "
                        "supersedes the binary-REDUCE assumptions in earlier "
                        "sections (--accumulator-design Q1 and --operand-layout)")
    p.add_argument("--time-muxed-sort", action="store_true",
                   help="print only the REVISED sort design : 1-stage time-"
                        "multiplexed sort network (16× smaller than full "
                        "pipeline, same latency, lower throughput). Supersedes "
                        "the area estimate in --sort-network")
    p.add_argument("--final-diagram", action="store_true",
                   help="print the FINAL full-system block diagram with all "
                        "revisions applied (unary REDUCE + RFS_B-as-acc + "
                        "512 B half tree + time-muxed sort + simplified "
                        "broadcast), plus REDUCE-vs-BROADCAST cost comparison "
                        "and cumulative area savings")
    p.add_argument("--softmax", action="store_true",
                   help="print only the softmax performance analysis : do we "
                        "need to specifically accelerate exp / div to match "
                        "cube unit GEMM throughput ? (spoiler: div=no, "
                        "exp depends on context length)")
    p.add_argument("--exp-unit", action="store_true",
                   help="print only the dedicated exp-unit design analysis : "
                        "i-BERT exp accuracy comparison and whether 32 copies "
                        "of dedicated exp is enough to match cube GEMM "
                        "(spoiler: yes for d_h ≥ 128 modern LLMs)")
    p.add_argument("--all-examples", action="store_true",
                   help="print 5 canonical examples (legacy default mode)")
    p.add_argument("--overview-only", action="store_true",
                   help="print only the datapath overview and exit")
    p.add_argument("--pseudo-only", action="store_true",
                   help="overview + tree + Benes diagram + pseudo-code "
                        "(skip per-shape examples)")
    return p.parse_args()


def main() -> None:
    args = parse_args()

    # ---- info-only modes ----
    if args.diagram:
        print_benes_diagram()
        return
    if args.format_tier:
        print_format_tier_design()
        return
    if args.benes_analysis:
        print_benes_analysis()
        return
    if args.operand_layout:
        print_operand_layout()
        return
    if args.sort_network:
        print_sort_network()
        return
    if args.accumulator_design:
        print_accumulator_design()
        return
    if args.topk_argmax:
        print_topk_argmax_design()
        return
    if args.unary_reduce:
        print_unary_reduce_revision()
        return
    if args.time_muxed_sort:
        print_time_muxed_sort_revision()
        return
    if args.final_diagram:
        print_final_block_diagram()
        return
    if args.softmax:
        print_softmax_analysis()
        return
    if args.exp_unit:
        print_dedicated_exp_design()
        return
    if args.overview_only:
        print_overview()
        return
    if args.pseudo_only:
        print_overview()
        print_tree_topology()
        print_benes_diagram()
        print_benes_analysis()
        print_operand_layout()
        print_accumulator_design()
        print_format_tier_design()
        print_topk_argmax_design()
        print_sort_network()
        print_unary_reduce_revision()
        print_time_muxed_sort_revision()
        print_final_block_diagram()
        print_softmax_analysis()
        print_dedicated_exp_design()
        print_pseudo_code()
        return

    # ---- legacy: dump all 5 canonical examples ----
    if args.all_examples:
        print_overview()
        print_tree_topology()
        print_benes_diagram()
        print_benes_analysis()
        print_operand_layout()
        print_accumulator_design()
        print_format_tier_design()
        print_topk_argmax_design()
        print_sort_network()
        print_unary_reduce_revision()
        print_time_muxed_sort_revision()
        print_final_block_diagram()
        print_softmax_analysis()
        print_dedicated_exp_design()
        print_pseudo_code()
        for in_spec, dw in DEFAULT_EXAMPLES:
            try:
                plan = make_plan(in_spec, double_width=dw)
                print_beat_trace(plan)
            except ValueError as e:
                print(f"  Skipping {in_spec} (double_width={dw}): {e}")
                print()
        return

    # ---- one-shot non-interactive ----
    if args.tile is not None:
        try:
            R, C, fmt = int(args.tile[0]), int(args.tile[1]), args.tile[2].upper()
        except ValueError:
            raise SystemExit("--tile expects: R (int) C (int) FMT (string)")
        if fmt not in FORMAT_E:
            raise SystemExit(f"--tile FMT must be one of {list(FORMAT_E)}")
        in_spec = TileSpec(R=R, C=C, fmt=fmt)
        plan = make_plan(in_spec, double_width=args.double_width)
    else:
        # ---- default: interactive prompt ----
        try:
            plan = interactive_input()
        except (EOFError, KeyboardInterrupt):
            print()
            print("  cancelled")
            return
        except (ValueError, SystemExit) as e:
            raise SystemExit(f"  input error: {e}")

    if args.with_context:
        print_overview()
        print_tree_topology()
        print_benes_diagram()
        print_benes_analysis()
        print_operand_layout()
        print_accumulator_design()
        print_format_tier_design()
        print_topk_argmax_design()
        print_sort_network()
        print_unary_reduce_revision()
        print_time_muxed_sort_revision()
        print_final_block_diagram()
        print_softmax_analysis()
        print_dedicated_exp_design()
        print_pseudo_code()
    print_beat_trace(plan)


if __name__ == "__main__":
    main()
