[package]
name = "pyc_runtime"
version = "0.1.0"
edition = "2021"
description = "pyCircuit Rust simulation runtime (v1 subset: Wire width 1..=64 + two-phase PycReg)"
license = "Apache-2.0"
publish = false

[lib]
path = "src/lib.rs"
